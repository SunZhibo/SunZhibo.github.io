<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Clockhands: Rename-free Instruction Set Architecture for Out-of-order Processors</title>
    <url>/posts/82c52e88.html</url>
    <content><![CDATA[<p>随着寄存器数量增加，处理器也由单发射架构转为多发射机构，但是受限制于数据竞争，内存读写，长周期指令等原因，顺序发射的数量存在瓶颈，因此引入了乱序发射。乱序发射的核心思想是
latency
hiding，当指令陷入停滞时允许后续的指令以乱序发射，继续执行，避免处理器的性能损失。</p>
<h2 id="寄存器重命名">寄存器重命名</h2>
<p>寄存器重命名则是解决了其中数据竞争的问题，在不考虑编译器优化的前提下，由于寄存器的数量有限，存在
RAW，WAR，WAW
三种类型的数据竞争（在乱序发射的情况下只要有写入的操作就可能存在竞争）。这一技术则是将物理寄存器映射到逻辑寄存器上，减少数据竞争的发生。汇编指令指代的为逻辑寄存器编号，根据具体的指令集可能存在不同的数量，而处理器将其映射到自身实际实现的物理寄存器上，并采取一定的映射策略，这样可以将数据竞争通过映射到不同寄存器的方法消除。</p>
<p>实现寄存器重命名则需要维护 rename
file，根据处理器的指令吞吐量有对应数量的读口和写口（RAM-like），而由于现代寄存器较多且多为64位宽，RAM
的逻辑大小又呈读写口的平方的级别增长，rename file
通常会占据较大的面积，并带来较大的功耗。</p>
<h3 id="编译器的寄存器优化">编译器的寄存器优化</h3>
<p>llvm的后端设计已经引入了寄存器逻辑编号的概念，在后端模型中有无数个可用的寄存器，在这个过程中编译器可以进行寄存器分配的优化，尽可能地减少寄存器数量限制带来的数据竞争。<strong>那为什么还需要硬件花费这么大的代价做寄存器的重命名呢？</strong>我认为一个可能的原因是：硬件在运行过程中的行为是一个不可预测的混沌系统，由中断、异常、分支预测、乱序发射带来的不确定性导致硬件重命名可以利用的动态信息大大超出编译器可利用的静态信息，优化效果自然更好，因此在编译器已经优化过的基础上，处理器仍然需要设计硬件的寄存器重命名结构。</p>
<h3 id="基于映射表的简单实现">基于映射表的简单实现</h3>
<p>最简单的实现方式是基于映射表，只需要在寄存器前维护一个映射关系表即可，这张表存储<strong>体系寄存器编号（即指令集对于寄存器的约定）</strong>到<strong>物理寄存器编号（处理器硬件实现）</strong>的映射关系。对于读请求，通过查询映射表返回物理寄存器的值，对于写请求，则维护一个可用映射的FIFO，每一次读请求都会建立一个新的逻辑寄存器到物理寄存器的映射关系，以消除
WAW 和 WAR 竞争。</p>
<p>为了解决 RAW
竞争，表中还需要维护该逻辑寄存器的状态，每个逻辑寄存器只会被更新一次（即创建该映射的写操作），因此只需要一个位维护是否处于就绪状态，当指令所有需要的操作数都就绪后允许发射，另外在
EX/WB
段也需要相应的广播机制来更新状态和数据。这一机制也有效保证了乱序执行的正确性，所有具有依赖关系的指令都会被就绪状态控制发射。为了解决
WAR 竞争，在 ISSUE
段需要存储当前指令映射到的物理寄存器编号，作为指令的一部分随流水线逐级流动。（ISSUE
QUEUE 的实现代价也是巨大的）</p>
<p>需要考虑的是异常或者分支预测错误的情况，此时流水线中执行了错误的指令，因此需要回滚映射表的状态，这就需要进行状态储存，或者依赖于提交阶段的信息进行回滚。基于此一个常见的优化是通过流水线划分，将分支预测结果在跳转指令执行前得出，避免起引起对映射状态的修改。</p>
<h3 id="基于保留站的寄存器重命名">基于保留站的寄存器重命名</h3>
<p>Tomasulo 算法中引入了 保留站（Reservation
station）和通用数据总线（Common Data
Bus）的概念，在每一个功能单元中加入了保留站，进一步地拆分为逻辑控制部分和功能执行部分。</p>
<p>指令首先由 instruction queue 进入到 issue queue
中，如果对应的执行单元的保留站中有空余，则发射到保留站中。保留站中的每一项都是一条发射了但尚未执行的指令，在保留站中的操作数可以分为两种情况：若该操作数的数据就绪，则直接写入到保留站的数据域中；否则则使用一个占位符来表示该数据是由哪个保留站中的指令产生的，当该指令执行完毕后会广播至CDB中，此时保留站则会更新状态至第一种情况。具体来说保留站中的每一项中包含：</p>
<ul>
<li><span class="math inline">\(op\)</span>：表示操作的类型</li>
<li><span class="math inline">\(Q_j, V_j, Q_k,
V_k\)</span>：表示两个操作数的状态，<span
class="math inline">\(Q\)</span>表示占位符编号，如果为0则表示数据已经就绪，<span
class="math inline">\(V\)</span>表示具体的数据域</li>
<li><span class="math inline">\(A\)</span>：内存读写操作的地址</li>
<li><span
class="math inline">\(busy\)</span>：表示该保留站标项的占用状态</li>
</ul>
<p>除此以外 register file
中也需要增加一个新的域，表示该寄存器的逻辑编号，如果没有就绪那么由哪一个保留站产出，便与后续发射指令更新占位符状态。在功能单元完成一条指令后，其结果会被广播，寄存器堆如果匹配到对应的编逻辑编号（即这是最后一条对该寄存器进行的写操作），那么就会更新其中的值。也就是说，如果寄存器的值不断地被写入，那么寄存器堆中并不会存储它一整串过程中的中间值，因为每一次新的写入操作都会修改该物理寄存器对应的逻辑寄存器编号，过程中的值只会在保留站中不断传递，这也给精确异常的实现带来了较大的困难。</p>
<p>为了实现精确异常，那么就需要保留一定的历史状态，首先一定要为乱序执行引入
reordered buffer
保证指令顺序入队，乱序发射，顺序提交，当发生投机失败或者异常中断时，可以根据
buffer
中的状态判断需要继续等待哪些指令完成，又有哪些状态需要回滚。而与之相对应的，需要讲
register file 的功能一分为二，新增 future file 作为原来的 register file
功能，而 register file
本身则依赖于指令的提交进行更新，相当于不断更新的历史状态备份。当需要回滚状态时，可以等待需要执行的指令提交完毕，然后回滚到
register file 的状态，重置所有的保留站状态。</p>
<p>保留站相当于使用了保留站的编号作为寄存器重命名的逻辑编号，使用寄存器堆中额外的状态域作为映射表。相比于基于映射表的实现方式，由于每个功能单元的保留站项数有限且没有多口读写的依赖，其逻辑代价非常小，也在
issue
段拥有更优秀的时序，主要的压力转移到广播式的数据总线上，多个不同的功能单元都要使用总线广播结果，导致其成为设计的瓶颈。</p>
<h2 id="clockshands-架构">clockshands 架构</h2>
<h3 id="新的寄存器重命名尝试">新的寄存器重命名尝试</h3>
<p>寄存器重命名无论采用何种实现方式都给硬件设计带来了巨大的逻辑复杂度和功耗压力，因此本篇文章尝试一种全新的架构解决这一问题。在这之前他们已经提出了一个叫做
straight
的指令集&amp;架构方案，指令集的操作数指明为是从该条指令向前数第几条指令产生的结果，相当于每一条指令都将结果写入一个新的寄存器，从编译器层面完成了寄存器重命名，同时规定一个最远限度<span
class="math inline">\(M\)</span>，指令的引用不得超过<span
class="math inline">\(M\)</span>条指令。在硬件方面，寄存器堆由一个 FIFO
ring buffer 实现，只保留最近的<span
class="math inline">\(M\)</span>个最近的寄存器结果，在消除了数据竞争的基础上，采用乱序多发射提高处理器性能，这样分支预测错误和异常都有较为简易地回滚方式。</p>
<p>这个指令集要求编译器做出较大的改动，并且编译出来的程序会有明显的膨胀，具体来说作者总结了三个使代码量显著增加的因素，都是由于<span
class="math inline">\(M\)</span>的限制引起：</p>
<ol type="1">
<li>循环变量保持：在多次循环中需要长期保持一些循环常量，这就要求编译器在其中添加大量的
move 指令</li>
<li>长生命周期变量：实际上也是和1情况类似，需要大量的 move 指令保持</li>
<li>汇合点处理：在多个分支的汇合点上，不同分支的历史指令情况不同，需要添加额外的指令让所有分支的上下文一致</li>
</ol>
<p>根据其他研究，结果生命周期为<span
class="math inline">\(x\)</span>的指令占比约为<span
class="math inline">\(O(\frac{1}{x^2})\)</span>，需要的 move
指令约为<span
class="math inline">\(\frac{k}{M}\)</span>，那么额外的指令数量约为<span
class="math inline">\(\sum_{1}^{P} O(\frac{1}{x^2}) * \frac{k}{M} =
O(\frac{1}{M}
lnP)\)</span>。实测下来至少有30%以上的代码行数增加，代价比较大。</p>
<h3 id="clockhands-isa">clockhands ISA</h3>
<p>clockhands 就是基于这个架构设计，为了解决指令数暴涨的问题，它基于
straight 的结构扩展为多个不同 group 的 ring
buffer，取操作数时需要指明是从哪一个组取最近<span
class="math inline">\(M\)</span>条指令的结果，写回结果时要指明写入了哪一个组。因此可以通过多个组的利用解决
straight
指令数量增加的问题，可以使用一个组专门存储长生命周期变量，经过实验选取了4组寄存器，最大距离16的配置，在编译器灵活性和硬件复杂性间取到平衡，编译器对四组寄存器做了特化处理：</p>
<ol type="1">
<li>临时变量存储在 t 组</li>
<li>长生命周期变量存储在 u 组</li>
<li>循环变量存储在 v 组</li>
<li>sp 寄存器专门放在 s 组，编译器优化只允许进行立即数加减</li>
</ol>
<p>因此硬件可以根据这一需求设计不同的寄存器组规格，例如增大 t
组的大小，减少 s 组的大小，并且可以适当增大 v
组的路径延时，在实际实现时并不会采用位移寄存器的方式移动组内所有的寄存器值，而是采用
counter 计数器变化来索引。在多发射过程中，issue
段就需要综合实际发射的指令数量动态变化距离，然后取到类似于保留站的占位符发射到功能单元中，在本文中作者添加了一个新的流水级来计算动态距离。对于乱序发射比较难办的错误恢复，clockhands
则是指需要根据 ROB 恢复四个组的计数器即可，乍一想没有太大问题。</p>
<h3 id="总结">总结</h3>
<p>总的来说，clockhands
这篇文章有点意思但是又感觉有意义的不多，这个架构确实可以解决寄存器重命名的设计缺点，但是对于整个生态架构的修改太大了，而且很难说这样修改后一定就能提升整体效率，因为非常多作用于传统架构的软硬件优化未必可以继续使用，而本文的实验又缺乏更大规模更大覆盖度的说服力，当个
idea 看就可以了。</p>
<p>参考链接</p>
<ol type="1">
<li><a href="https://en.wikipedia.org/wiki/Register_renaming">Register
Renaming</a></li>
<li><a
href="https://en.wikipedia.org/wiki/Tomasulo%27s_algorithm">Tomasulo
Algorithm</a></li>
</ol>
]]></content>
      <categories>
        <category>Paper Note</category>
      </categories>
      <tags>
        <tag>CPU</tag>
        <tag>Architecture</tag>
      </tags>
  </entry>
  <entry>
    <title>CodeForces Record</title>
    <url>/posts/bd585dfa.html</url>
    <content><![CDATA[<h1 id="cf-round802-div.2">CF Round#802 Div.2</h1>
<p><a href="https://codeforces.com/contest/1700">Codeforces Round#802
Div2</a></p>
<h2 id="a.-optimal-path">A. Optimal Path</h2>
<p><code>题意</code>：给一个<span
class="math inline">\(n*m\)</span>的矩阵，<span
class="math inline">\(a(i, j) = (i - 1)m + j\)</span>，求一条从<span
class="math inline">\((1, 1)\)</span>到<span class="math inline">\((n,
m)\)</span>的最小权值路径。</p>
<p><code>题解</code>：总步数是确定的，横向移动和竖向移动分别会为后面的每一步增添一个额外的代价，贪心做一下。</p>
<h2 id="b.-palindromic-numbers">B. Palindromic Numbers</h2>
<p><code>题意</code>：给定一个大整数，求另一个与之长度相同的数，使得二者之和为回文数。</p>
<p><code>题解</code>：构造，如果首位是9，那么一定可以满足回文数为全1，否则构造全9的回文数。</p>
<h2 id="c.-helping-the-nature">C. Helping the Nature</h2>
<p><code>题意</code>： 给定数组<span
class="math inline">\(a\)</span>，可以使一个前缀或一个后缀都减一，可以使数组整体加一，求最少次数使得数组元素变为完全相同。</p>
<p><code>题解</code>：
转换为对差分数组操作，就变成了单点加一和减一，和不修改，前两种是唯一可以改变差分数组的操作，要让差分数组变为全0，再考虑整体需要加或者减几次。</p>
<h2 id="d.-river-locks">D. River Locks</h2>
<p><code>题意</code>：
有若干水闸，前面的水满了会溢到后面去，每个闸有自己的容量，多次询问若要求<span
class="math inline">\(t\)</span>时间内将所有水闸放满，至少需要打开几个闸门的入水口。</p>
<p><code>题解</code>：
注意到前面的水可以留到后面，后面不能去前面，所以一定是打开最前面若干个闸门的入水口，因此不断对前缀做判断即可。这里注意要规避掉某个极大的闸门在前面，放满它的时间是整体时间的平静。</p>
<h2 id="e.-serega-the-pirate">E. Serega the Pirate</h2>
<p><code>题意</code>： 有一个<span
class="math inline">\(n*m\)</span>的矩阵，里面填有<span
class="math inline">\(1\)</span>到<span
class="math inline">\(n*m\)</span>，从<span
class="math inline">\(1\)</span>开始走，可以走重复的格，但是要严格按照升序<span
class="math inline">\(1, 2, 3
...\)</span>走，求是否有解，如果没有的话求有几种方案可以交换两个数的位置使得存在解。</p>
<p><code>题解</code>：
做题的时候想到的是并查集来判断解存不存在，但是很脑筋急转弯的是只要每个点都存在一个相邻的更小的值，那么就一定存在解，可以归纳证明。</p>
<p>基于此考虑没解的情况下怎么求方案数，交换两个点只会影响交换的两个点周围的点以及本身，那么找一个周围都更大的点<span
class="math inline">\(x\)</span>，可能的方案只可能是一个点在<span
class="math inline">\(x\)</span>及周围，另一个在<span
class="math inline">\(x+1\)</span>附近，因此暴力枚举方案，依次做暴力验证即可，注意这里可能会有重复的方案。</p>
<h2 id="f.-puzzle">F. Puzzle</h2>
<p><code>题意</code>： 给出两个<span
class="math inline">\(2*n\)</span>的01矩阵，可以交换两个相邻位置的数，求最少次数使得矩阵1变为矩阵2。</p>
<p><code>题解</code>：
乍一看像是dp，但是不太好描述状态。考虑一个退化的情况，如果只有一行，那么直接按顺序分配即可，那么扩展到两行。不妨假定把竖向的操作提到最前面，已经完成了竖向的操作，那么两行内就直接按顺序分配即可。</p>
<p>这道题有个很新颖的想法就是换一种计算一行内情况的方式，正好是01数组，两个数组的前缀和做差，再求绝对值的和就是需要的操作次数。</p>
<p>那么在这个基础上考虑，纵向的移动相当于把一行的前缀和的后缀减一，另外一行的前缀和的后缀加一，考虑怎么让这个和最小。</p>
<p>考虑从左到右扫描，在某一列遇到了1，记第一行前缀和差为<span
class="math inline">\(f_{1}\)</span>，第二行前缀和差为<span
class="math inline">\(f_{2}\)</span>，如果<span
class="math inline">\(f_{1}\)</span>和<span
class="math inline">\(f_{2}\)</span>异号，不妨假设<span
class="math inline">\(f_{1} \gt 0, f_{2} \lt
0\)</span>，那么说明第一行前面有多余的1，第二行缺少了1，这是把第一行的1移动下来一定是最优的。</p>
<p>证明如下，考虑一个情况如下矩阵，那么在右上角及其右侧的第一行对于1的需求由矩阵中任何的1移动而来需要的代价是相同的，左下角及其左侧也是一样的。</p>
<p><span class="math display">\[\begin{pmatrix}
1&amp;0\\
0&amp;1\\
\end{pmatrix}\]</span></p>
<p>其他的情况可以手玩一下，可以得出类似的证明，而且当<span
class="math inline">\(f_{1}\)</span>和<span
class="math inline">\(f_{2}\)</span>异号时，一定是刚刚变为异号，即绝对值为1，因此这种情况下把多余的1纵向交换到另一行一定是最优解，贪心从左到右扫描即可。</p>
<h1 id="cf-round930-div.1">CF Round#930 Div.1</h1>
<p><a href="https://codeforces.com/contest/1936">Codeforces Round#930
Div1</a></p>
<h2 id="a.-bitwise-operation-wizard">A. Bitwise Operation Wizard</h2>
<p><code>题意</code>：已知<span
class="math inline">\(n\)</span>，有一个未知的<span
class="math inline">\(n\)</span>的排列<span
class="math inline">\(p\)</span>，可以交互查询<span
class="math inline">\(p_{a} | p_{b}\)</span>与<span
class="math inline">\(p_{c} | p_{d}\)</span>的大小关系，在<span
class="math inline">\(3n\)</span>次内找出任意一组答案<span
class="math inline">\(x, y\)</span>使得<span class="math inline">\(p_{x}
\oplus p_{y}\)</span>最大。</p>
<p><code>题解</code>：最后的异或没看到被坑惨了，题意玩一下，查询可以直接用来比大小，那么先找到最大值，一定存在一组答案是由最大值<span
class="math inline">\(p_{x}\)</span>参与的，另一个数要满足跟它二进制互补，但是查询只有或。第一次查询找到所有或起来最大的<span
class="math inline">\(p_{y}\)</span>集合，他们一定弥补了<span
class="math inline">\(p_{x}\)</span>二进制中所有的0，那么在其中找最小值就是不含有<span
class="math inline">\(p_{x}\)</span>中所有的1。</p>
<h2 id="b.-pinball">B. Pinball</h2>
<p><code>题意</code>：一个球在一个一维平面内滚动，每个位置表示球的滚动方向'&gt;'或者'&lt;'，滚动后这一位置会变化为反向，问从每个位置开始滚动需要多久才可以滚出去。</p>
<p><code>题解</code>：手玩一下，球的运动路径就是沿着一个方向一直走，碰到反向前形成了一条反向的路径，反向后在另一个方向重复这个过程，实际上就是反向到球出发的距离和的两倍。因此，考虑一下球最后的落点，这个方向只会走一次，顺便维护一下左侧的'&gt;'和右侧的'&lt;'即可。</p>
<h2 id="c.-pokémon-arena">C. Pokémon Arena</h2>
<p><code>题意</code>：<span
class="math inline">\(n\)</span>个人打比赛，每个人有<span
class="math inline">\(m\)</span>维的属性和出场费<span
class="math inline">\(c_{i}\)</span>，任何一个人只要某一维不低于对手就可以战胜他，初始1号在擂台上，战胜他就可以替代他守擂，你可以花费等量的钱来为任意选手永久提升任意的属性，求最小花费的方案使得第<span
class="math inline">\(n\)</span>个人打上擂台。</p>
<p><code>题解</code>：首先分析一下这个背景，可以得到两个结论：</p>
<ul>
<li>永久属性提升可以视作一次性提升：考虑你通过比较一个提升过的属性来攻擂，那么直接提升属性到攻擂者一定不会更差</li>
<li>一位选手不会多次登台：既然可以看作一次性提升，那么如果出现选手两次攻擂，直接消除掉中间的过程答案一定更优</li>
</ul>
<p>然后发现就可以去掉dp的后效性了，直接做一个dp：f[i]表示第i人守擂的最小花费。可以发现dp的转移是不定向的，那么直接建图跑最短路。进而发现这个图边集是爆炸的，每个状态都有<span
class="math inline">\(nm\)</span>个转移方式，那就要优化建图，直接拆点，把每个维度按照数值排序，只在相邻数值的点对间建边，那么就可以把边的数量压缩到<span
class="math inline">\(nm\)</span>级别，最后跑一个最短路得到f[n]。</p>
<h2 id="d.-bitwise-paradox">D. Bitwise Paradox</h2>
<p><code>题意</code>：给定两个数组<span class="math inline">\(a,
b\)</span>和一个常数<span
class="math inline">\(v\)</span>，定义一个子区间<span
class="math inline">\([l, r]\)</span>是好的，当满足<span
class="math inline">\(b_{l} | ... | b_{r} \geq
v\)</span>，定义区间得权值为<span class="math inline">\(max(a_{l}, ..,
a_{r})\)</span>。支持两种操作，单点修改<span
class="math inline">\(b\)</span>，区间查询好的子区间的权值中的最小值。</p>
<p><code>题解</code>：看着就很像线段树，但是很难决定区间去维护什么，遇事不决就直接维护答案，考虑如何合并两个线段上的节点。父节点的答案取子节点的最小值，再考虑跨区间的情况.</p>
<p>或操作从高位按位考虑，只考虑更高位是紧约束的情况，分情况讨论：</p>
<ul>
<li><p>如果<span
class="math inline">\(v\)</span>这一位是0，那么跨区间如果取了1，低位再怎么取都没有约束了，直接按照取1带来的约束更新答案，如果跨区间取了0，相当于没有新的约束增加，继续枚举下一位。</p></li>
<li><p>如果<span
class="math inline">\(v\)</span>这一位是1，那么跨区间必须取1，更新约束继续。</p></li>
</ul>
<p>幸好权值为<span
class="math inline">\(a\)</span>的区间最大值，因此取1带来的约束就是一次最大值的更新，不同位置的1带来的约束是互不影响的，可以按位独立考虑。那么如果要取1，如何能让跨区间的权值最小呢，那就是跨区间包含了左节点区间中最右侧的1，或者包含右节点区间中最左侧的1，那么额外维护一下这个信息就可以在<span
class="math inline">\(O(logV)\)</span>时间内合并两个区间了。这里<span
class="math inline">\(a\)</span>的区间最值需要用RMQ与处理一下，总复杂度为<span
class="math inline">\(O(qlogNlogV)\)</span>。</p>
<h1 id="cf-round934-div.2">CF Round#934 Div.2</h1>
<p><a href="https://codeforces.com/contest/1944">Codeforces Round#934
Div2</a></p>
<h2 id="a.-destroying-bridges">A. Destroying Bridges</h2>
<p><code>题意</code>：给一张完全图，求删去<span
class="math inline">\(k\)</span>条边后，点1不可到达的编号最大的点。</p>
<p><code>题解</code>：手玩一下最优解盯着点1的边删就行。</p>
<h2 id="b.-equal-xor">B. Equal XOR</h2>
<p><code>题意</code>：将2个n的排列打乱为等长的两部分，各选出<span
class="math inline">\(k\)</span>个值，使得两边的异或和相同。</p>
<p><code>题解</code>：构造，如果两个一样的数在一侧直接选，如果还剩就从异侧里面补。</p>
<h2 id="c.-mex-game-1">C. MEX Game 1</h2>
<p><code>题意</code>：博弈，轮流从数组里面取数，使得Alice得到的数组的mex最大。</p>
<p><code>题解</code>：发现从0开始，如果有至少两个数，那么一定是不急着拿的，只考虑出现一次的数，第一个可以先手直接拿，第二个就会成为瓶颈。</p>
<h2 id="d.-non-palindromic-substring">D. Non-Palindromic Substring</h2>
<p><code>题意</code>：定义字符串为<span
class="math inline">\(k-good\)</span>，即存在长度为<span
class="math inline">\(k\)</span>的非回文子串，定义字符串的权重为所有<span
class="math inline">\(k-good\)</span>的<span
class="math inline">\(k\)</span>之和，多次区间查询子串的权重。</p>
<p><code>题解</code>：这个<span
class="math inline">\(good\)</span>是一个非常强的约束，当<span
class="math inline">\(k\)</span>为偶数时，要求字符串完全由一个字符组成，当<span
class="math inline">\(k\)</span>为奇数时，要求字符串由两个字符交替组成，特别地当<span
class="math inline">\(k =
len(str)\)</span>时需要特判。因此本题转换为了多次对区间子串求是否为回文串，manacher解决。</p>
<h2 id="e.-tree-compass">E. Tree Compass</h2>
<p><code>题意</code>：定义染色操作，对树的任一节点和任一距离<span
class="math inline">\(d\)</span>，染色所有到该点距离为<span
class="math inline">\(d\)</span>的点，求一棵树的最小染色次数。</p>
<p><code>题解</code>：很容易想到树的直径，讨论一下链的退化情况，按照链的长度分为：奇数，模4余2，模4余0，共三类情况，并可以给出由中心开始的染色方案。扩展到树的普通情况，基于链的性质，链外的路径一定不会更长，因此从中心开始的染色方案一定可以覆盖直径以外的节点，固可按照直径链的退化情况进行构造。</p>
<h2 id="f.-counting-is-fun">F. Counting Is Fun</h2>
<p><code>题意</code>：对于一个数组，可以选择一段长度大于1的子数组进行区间减操作，如果一个数组可以通过若干次操作变为全0数组，则称其为好的。求长度为<span
class="math inline">\(n\)</span>的，初始元素范围在<span
class="math inline">\([0, k]\)</span>中的好数组一共有多少个。</p>
<p><code>题解</code>：</p>
<ul>
<li><p>首先要把这个好的定义转换一下，主要的限制来源于长度要大于1，这意味着需要满足<span
class="math inline">\(a_{i} \leq a_{i-1} +
a_{i+1}\)</span>，对于数组两端可以认为<span
class="math inline">\(a_{0}=a_{n+1}=0\)</span>，满足这个条件即为好数组。</p></li>
<li><p>然后考虑统计，一个简单的想法是dp，状态一定要有<span
class="math inline">\(k^2\)</span>表示这个约束，时间复杂度太大，那么这种计数问题通常就要往容斥原理的方向考虑，定义<span
class="math inline">\(a_{i} \ge a_{i-1}+a{i+1}\)</span>时，位置<span
class="math inline">\(i\)</span>是坏的。定义<span
class="math inline">\(f(x)\)</span>表示集合<span
class="math inline">\(x\)</span>中所有位置都是坏的的数组个数，那么根据容斥原理好的数组总数为<span
class="math inline">\(\sum (-1)^{|x|}f(x)\)</span>。</p></li>
<li><p>接下来考虑怎么求这个容斥，<span
class="math inline">\(f[i][j][0/1]\)</span>表示：<span
class="math inline">\(a_{i}=j\)</span>，考虑<span
class="math inline">\([1,
i)\)</span>位置中有至少奇数/偶数个坏位置的方案数，那么转移方程如下：</p>
<ul>
<li><p>位置<span
class="math inline">\(i-1\)</span>无所谓是不是坏的（容斥原理）：<span
class="math inline">\(f[i][j][l] = \sum f[i-1][j][l]\)</span></p></li>
<li><p>位置<span class="math inline">\(i-1\)</span>一定是坏的：<span
class="math inline">\(f[i][j][l] = \sum f[i-2][j&#39;][l^1] *
(k-j&#39;-j)\)</span></p></li>
</ul></li>
<li><p>dp式前缀和优化一下就可以得到<span
class="math inline">\(O(n^2)\)</span>的写法。</p></li>
</ul>
<h1 id="cf-round941-div.2">CF Round#941 Div.2</h1>
<p><a href="https://codeforces.com/contest/1966">Codeforces Round#941
Div2</a></p>
<h2 id="a.-card-exchange">A. Card Exchange</h2>
<p><code>题意</code>：给定<span
class="math inline">\(n\)</span>个数，可以选择<span
class="math inline">\(k\)</span>个相同的数变为<span
class="math inline">\(k-1\)</span>个任意数，求操作后最少剩几个数。</p>
<p><code>题解</code>：只要能开始操作，答案就是1。</p>
<h2 id="b.-rectangle-filling">B. Rectangle Filling</h2>
<p><code>题意</code>：给定一个二维黑白染色矩阵，操作是选择两个颜色相同的块，将这两个块对应的矩形染为这个颜色，求是否能将矩阵染成纯色。</p>
<p><code>题解</code>：手玩一下，不能染为纯色的充要条件是黑色和白色各站住了对侧最边缘的一条（左右/上下）。</p>
<h2 id="c.-everything-nim">C. Everything Nim</h2>
<p><code>题意</code>：一堆石子，每次要从所有所有堆中拿走一样多的石子，不能操作的输。</p>
<p><code>题解</code>：差分一下变成从一堆石子里面取，然后倒着推一下状态，只考虑数量是否为1就可以。</p>
<h2 id="d.-missing-subsequence-sum">D. Missing Subsequence Sum</h2>
<p><code>题意</code>：构造一个数组，子序列的和可以覆盖<span
class="math inline">\([1,n]\)</span>，但是不能含有<span
class="math inline">\(k\)</span>。</p>
<p><code>题解</code>：首先构造<span
class="math inline">\([1,k-1]\)</span>，类似二进制的方式构造，最后一步跟<span
class="math inline">\(k\)</span>做差算一下，然后后面很简单地缺什么加什么，例如得到<span
class="math inline">\([1,k-1]\)</span>后添加<span
class="math inline">\(k+1\)</span>，子序列和对应<span
class="math inline">\([1, 2k]\)</span>，然后添加<span
class="math inline">\(2k+1\)</span>，注意这次开始会引入新的缺陷<span
class="math inline">\(3k+1\)</span>，然后逐次添加缺陷即可，会发现缺陷每次会倍增，因此构造的数组不会超过<span
class="math inline">\(log2_{2}\)</span>级别。</p>
<h2 id="e.-folding-strip">E. Folding Strip</h2>
<p><code>题意</code>：一个01纸带，定义合法的折叠是指折叠后空间上重合的纸袋数字相同，求经过合法折叠后纸带最短的长度。</p>
<p><code>题解</code></p>
<ul>
<li><p>看着很像回文串相关，区间dp相关，但是仔细想做不了一点，全是后效性和影响。因此尝试找性质，提出一种可能：在每个相邻的相同数字之间都折一下，是一种合法的折叠方案。证明从奇偶性考虑，这样折叠后按照下标考虑，所有的0和1奇偶性一定不同（相同的就被折叠了），因此在空间上重叠的一定都相同，方案合法。这样折叠之后不可能再次折叠了，而且通过回文的性质可知，局部的回文折叠不会影响更长的回文串，因此这个折法是最优方案。</p></li>
<li><p>考虑怎么求解最后剩余的纸带长度，从空间上一层一层摊开，可以认为纸带先往一个方向延申，遇到需要折叠的地方再调转方向，这样就可以模拟一个数轴，记录纸带到达的最左侧和最右侧，做差即为折叠后并在一起的纸带的长度。实际代码特别短，这道题很有意思。</p></li>
</ul>
<h2 id="f.-missing-subarray-sum">F. Missing Subarray Sum</h2>
<p><code>题意</code>：有一个回文数组，给出所有子数组的和，但是去掉了其中一个，尝试还原数组。</p>
<p><code>题解</code>：回文的性质很好地限制了数组的构成，但是不太好找出被删掉的数是什么。那么分情况讨论一下：如果数组是奇数长度<span
class="math inline">\(2k+1\)</span>，那么子数组的和构成的序列中，应该会有<span
class="math inline">\(k+1\)</span>个出现过奇数次的和，且保持一样的奇偶性；如果数组是偶数长度<span
class="math inline">\(2k\)</span>，应该会有<span
class="math inline">\(k\)</span>个出现过奇数次的和，且都为偶数。那么就可以根据这个性质得出，删掉的数到底是本应出现奇数次的中心对称回文的和，还是一个本应出现偶数次的和。</p>
<p>分情况讨论，如果删掉的是本应出现奇数次的和，也就是删掉了一个中心对称的回文和：那么我们先当作没有删，去重建这个数组，可以发现相当于得到的数组相比于原数组，会有两个数合在了一起，对应着被删掉的和的位置。举例来说原数组在<span
class="math inline">\(a_{x}=3,a_{x+1}=2\)</span>，那么在重建数组中只会得到<span
class="math inline">\(a_{x}=5\)</span>，因为区间和<span
class="math inline">\(a_{x+1,
n}\)</span>被删掉了。同样地，如果是删掉了偶数次的和，那么会有一个数被拆开成两个数。</p>
<p>那么我们比较重建数组的子数组和与给出的删掉一个数的原数组和，排序后第一个不同的和<span
class="math inline">\(t\)</span>就代表着两个数组的差异。对于两个数合二为一的情况，说明原数组的和刚好只包含了两个数之一，对于拆开的情况，则是重建的和包含了拆开的两个数之一。无论哪个情况，这个和都包含着回文数组和的一半，再加上从中心到达两个数之一的子数组之和。</p>
<p>如果删除的不是数组的最大值<span
class="math inline">\(sum\)</span>，那么就可以通过<span
class="math inline">\(2 * t -
sum\)</span>来算出删掉的和，补全后重建即可。如果删除的正好是数组的最大值，那么仍然这么做，可以发现<span
class="math inline">\(t=a_{1, n -
1}\)</span>，依然可以还原出删去的值。</p>
<h1 id="cf-round953-div.2">CF Round#953 Div.2</h1>
<p><a href="https://codeforces.com/contest/1978">Codeforces Round#953
Div2</a></p>
<h2 id="a.-alice-and-books">A. Alice and Books</h2>
<p><code>题意</code>：给定<span
class="math inline">\(n\)</span>个数，分为两堆，每堆取编号最大的，求最大的和为多少。</p>
<p><code>题解</code>：答案即为前<span
class="math inline">\(n-1\)</span>个数中的最大值加上第<span
class="math inline">\(n\)</span>个数。</p>
<h2 id="b.-new-bakery">B. New Bakery</h2>
<p><code>题意</code>：卖<span
class="math inline">\(n\)</span>件货物，可以选定一个<span
class="math inline">\(k\)</span>，前<span
class="math inline">\(k\)</span>件卖出的货物中的第<span
class="math inline">\(i\)</span>件以<span
class="math inline">\(b-i+1\)</span>的价格卖出，其余按照固定价<span
class="math inline">\(a\)</span>卖出，求最大获利。</p>
<p><code>题解</code>：想办法卖高价，<span class="math inline">\(k \leq b
- a\)</span>，考虑一下边界条件。</p>
<h2 id="c.-manhattan-permutations">C. Manhattan Permutations</h2>
<p><code>题意</code>：定义排列的曼哈顿距离为<span
class="math inline">\(\sum |p_i - i|\)</span>，构造距离为<span
class="math inline">\(k\)</span>的<span
class="math inline">\(n\)</span>的排列。</p>
<p><code>题解</code>：手玩一下，曼哈顿距离一定为偶数，且最大的情况为倒序排列。考虑构造，从正序排列开始，如果<span
class="math inline">\(k\)</span>够大则交换首位元素，否则交换首元素与某一中间元素停止。这样的交换方法不会对序列中间的距离产生影响，可以方便地缩小问题的规模。</p>
<h2 id="d.-elections">D. Elections</h2>
<p><code>题意</code>：<span
class="math inline">\(n\)</span>个人选举，每个人有<span
class="math inline">\(a_i\)</span>选民，还有<span
class="math inline">\(c\)</span>个不定选民会投给编号最小的候选人，当平票时选编号最小的当选。你可以选择一部分候选人出局，他们的选民会成为不定选民，问每个候选人当选最少需要出局多少人。</p>
<p><code>题解</code>：考虑胜选的情况：如果不需要不定选民投给你，那么就要满足<span
class="math inline">\(a_{x} &gt; a_{1} +
c\)</span>，否则就一定需要不定选民，那么<span class="math inline">\([1,
x)\)</span>的候选人一定要出局，若还不够再考虑从后面贪心选大的出局。</p>
<h2 id="e.-computing-machine">E. Computing Machine</h2>
<p><code>题意</code>：给定两个01串<span class="math inline">\(s,
t\)</span>，若<span
class="math inline">\(s_{i}=s_{i+2}=0\)</span>则可使<span
class="math inline">\(b_{i}=1\)</span>，若<span
class="math inline">\(b_{i}=b_{i+2}=1\)</span>则可使<span
class="math inline">\(a_{i}=1\)</span>，多次查询区间<span
class="math inline">\(l,
r\)</span>，对这个区间的字串进行操作最多可得到多少个<span
class="math inline">\(s\)</span>串中的1。</p>
<p><code>题解</code>：字串只有边缘会受到影响，中间的结果与两个串直接操作没区别，边缘单独判断一下即可。</p>
<h2 id="f.-large-graph">F. Large Graph</h2>
<p><code>题意</code>：给定一个数组<span
class="math inline">\(a\)</span>，循环右移得到<span
class="math inline">\(n * n\)</span>矩阵<span
class="math inline">\(b\)</span>，由矩阵<span
class="math inline">\(b\)</span>生成一张图，每个点代表矩阵中一个元素，两个点有边当两点在矩阵中曼哈顿距离不超过<span
class="math inline">\(k\)</span>，且两个点在矩阵代表的值不互质，求图中连通块的数量。</p>
<p><code>题解</code>：当<span class="math inline">\(k \geq
2\)</span>时，斜着的条状带构成了<span class="math inline">\(2n -
1\)</span>个连通块，问题退化为一维，那么对于每一个质因数，添加连向最近的公共因子的边，这样可以控制每一个质因子的边的数量不超过<span
class="math inline">\(O(N)\)</span>，直接搜索即可。稍微注意一下当<span
class="math inline">\(a_i =
1\)</span>时，每个点都是独立的连通块，然后<span
class="math inline">\(ans\)</span>是<span
class="math inline">\(O(n^2)\)</span>级别的，会暴int，最后质数只要枚举到根号级别就可以，剩下的做个map搞一下。</p>
<h1 id="cf-round959-div.1div.2">CF Round#959 Div.1+Div.2</h1>
<p><a href="https://codeforces.com/contest/1994">Codeforces Round#959
Div1+Div2</a></p>
<h2 id="a.-diverse-game">A. Diverse Game</h2>
<p><code>题意</code>：构造一个排列与给出的排列每一位都不相同。</p>
<p><code>题解</code>：每位+1输出即可。</p>
<h2 id="b.-fun-game">B. Fun Game</h2>
<p><code>题意</code>：给定两个01串<span class="math inline">\(s,
t\)</span>，每次操作可以选定一个子区间<span class="math inline">\(l,
r\)</span>，替换<span class="math inline">\(s_{i} = s_{i} \ xor \  s_{i
- l + 1}\)</span>，问是否可以通过操作使<span
class="math inline">\(s\)</span>变成<span
class="math inline">\(t\)</span>。</p>
<p><code>题解</code>：考虑前缀，异或自己1可以变成0，但是0无法变成1，如果一直到<span
class="math inline">\(t\)</span>串第一个1出现为止<span
class="math inline">\(s,
t\)</span>都可以匹配，那么后面任何想要更改的位都可以通过这个1来逐位操作。</p>
<h2 id="c.-hungry-games">C. Hungry Games</h2>
<p><code>题意</code>：给定一个数组<span
class="math inline">\(a\)</span>，代表蘑菇的毒性，如果要吃掉一个区间<span
class="math inline">\(l,
r\)</span>的蘑菇，会按照顺序吃，毒性每累积到<span
class="math inline">\(x\)</span>或更高，毒性会刷新清零，问有多少个字区间吃完后毒性累积不为0。</p>
<p><code>题解</code>：对于每一个点求出到哪里毒性清零，然后从后往前递推一下即可。</p>
<h2 id="d.-funny-games">D. Funny Games</h2>
<p><code>题意</code>：给定一个数组<span
class="math inline">\(a\)</span>，进行<span
class="math inline">\(n-1\)</span>次操作，每<span
class="math inline">\(i\)</span>次操作可以任选一个<span
class="math inline">\(u, v\)</span>满足<span class="math inline">\(|a_u
- a_v| \equiv 0 \  mod \  i\)</span>，会添加一条<span
class="math inline">\(u,
v\)</span>之间的边。尝试给出一种方案使得图连通。</p>
<p><code>题解</code>：样例猜个结论，一定有可行解，根据抽屉原理，模数总会有两个相同的，因此从<span
class="math inline">\(n-1\)</span>开始倒序构造，每次任选两个模数相同的点连起来即可。</p>
<h2 id="e.-wooden-game">E. Wooden Game</h2>
<p><code>题意</code>：给出<span
class="math inline">\(n\)</span>棵树，每次可以任选一棵砍掉一个子树，记其大小为<span
class="math inline">\(x\)</span>，求可以得到的最大的<span
class="math inline">\(\sum xor(x)\)</span>。</p>
<p><code>题解</code>：一棵大小为<span
class="math inline">\(x\)</span>的树可以得到<span
class="math inline">\([1,
x]\)</span>的任意结果，只需要一个一个的砍掉，直到剩下的大小为需要的结果。那么逐位构造答案，如果只有一棵树的大小这一位为1，那么直接减去这个大小继续构造，如果多于一棵则后续所有位都可以为1。</p>
<h2 id="f.-stardew-valley">F. Stardew Valley</h2>
<p><code>题意</code>：给定一张无向图，有一部分边必须要选，一部分边可选，找出一条欧拉回路。</p>
<p><code>题解</code>：必须选的边会导致一部分点的度数为奇数，但是加边不太好加，换一个思路从所有边往下删边，删非必选的边。</p>
<p>只考虑非必选边，搜索出一棵生成树，自下而上地遍历，若点的度数为奇数，删去与父亲连接的边，这样总能调整到所有的点都为偶数。（因为度数总和一定为2的倍数，所以根节点的度数奇偶性也可以得到保证）又由于图中保证了必选边的连通性，得到的图一定具有欧拉回路。</p>
<h2 id="g.-minecraft">G. Minecraft</h2>
<p><code>题意</code>：给定<span
class="math inline">\(n\)</span>个数，求一个数<span
class="math inline">\(x\)</span>使得<span class="math inline">\(\sum
a_{i} \ xor \ x =
s\)</span>，其中数为大数，以01串形式给出，01串的长度为<span
class="math inline">\(k\)</span>。</p>
<p><code>题解</code>：先不考虑复杂度，很显然的想法就是按位考虑dp，但是问题是状态数目爆炸，想办法压缩状态。观察发现有效的状态其实非常少，因为<span
class="math inline">\(x\)</span>的这一位会造成<span
class="math inline">\(n-cnt_i\)</span>或者<span
class="math inline">\(cnt_i\)</span>的贡献，所以有效的状态不超过<span
class="math inline">\(n\)</span>个，状态就可以被压缩到<span
class="math inline">\(O(nk)\)</span>，直接dp即可。</p>
<h2 id="h.-fortnite">H. Fortnite</h2>
<p><code>题意</code>：交互题，最多三次查询找出参数<span
class="math inline">\(p,
m\)</span>，可以查询一个小写字母字符串，返回<span
class="math inline">\((\sum ord(s_i)p^i) \% m\)</span>，保证<span
class="math inline">\(27 \le p \leq 50, \ p + 2 \leq m\)</span></p>
<p><code>题解</code>：第一次查询<span
class="math inline">\(aa\)</span>得到<span
class="math inline">\(p\)</span>，第二次查询一定是构造一个足够大的串（至少要大于<span
class="math inline">\(m\)</span>），关键是第三次怎么得到<span
class="math inline">\(m\)</span>。</p>
<p>这种构造题，范围又给的很刻意，就会有一些神秘的性质。一个显然的思路是这样：记第二次查询的原始值为<span
class="math inline">\(h_1\)</span>，答案为<span
class="math inline">\(a_1\)</span>，那么显然<span
class="math inline">\(h_1 - a_1 =
km\)</span>。那么如果第三次查询的原始值<span
class="math inline">\(h_2\)</span>满足<span class="math inline">\(h_1 -
a_1 - 1 \geq h2 \geq h_1 - a_1 - m\)</span>，那么就一定有<span
class="math inline">\(h_2 - a_2 = (k - 1)m\)</span>，即得到<span
class="math inline">\(m = (h_1 - a_1) - (h_2 -
a_2)\)</span>。这个倒是不难想，关键是如何构造这样的<span
class="math inline">\(h_2\)</span>，注意到在这个<span
class="math inline">\(p\)</span>进制下，有些数字可能是表示不了的，因为小写字母只有1到26。</p>
<p>题解给了一种利用性质的构造方法，首先令第一次查询的串为<span
class="math inline">\(zzzzzzz\)</span>，我们不妨先不考虑1到26的限制，将<span
class="math inline">\(a_1 + 1\)</span>写为<span
class="math inline">\(p\)</span>进制数，即<span
class="math inline">\(a_1 + 1 = \sum_{i = 0}^{i &lt; len} d_i \
p^i\)</span>，然后我们用<span
class="math inline">\(h_1\)</span>减去它得到一个可能不合法的<span
class="math inline">\(p\)</span>进制数。记不合法的下标集合为<span
class="math inline">\(S\)</span>，对于每个<span class="math inline">\(i
\in S\)</span>，令<span class="math inline">\(d_i = 26, \ d_{i+1} = d_{i
+ 1} - 1\)</span>，得到一个合法的<span
class="math inline">\(p\)</span>进制串作为第三次查询。这样的构造一定能满足<span
class="math inline">\(h_1 - a_1 - 1 \geq h2 \geq h_1 - a_1 -
m\)</span>，证明如下。</p>
<p>首先新构造出的值一定会更小，因为借位操作<span class="math inline">\(p
\geq 27\)</span>，因此满足<span class="math inline">\(h_2 \leq h_1 - a_1
-1\)</span>。考虑发生借位的情况，借位操作会减去一个<span
class="math inline">\(p^{i+1}\)</span>，增加一个<span
class="math inline">\(d_i \ p^i\)</span>，因此借位操作会让构造出的<span
class="math inline">\(h_2\)</span>在<span class="math inline">\(h_1 -a_1
-1\)</span>的基础上变小<span class="math inline">\((p -
d_i)p^i\)</span>。又因为发生借位的情况一定是<span
class="math inline">\(d_i \geq 26\)</span>，因此变小的差值为<span
class="math inline">\(\delta = \sum_{i \in S} (p - d_{i}) p^{i} \leq
\sum_{i \in S} 24p_{i}\)</span></p>
<p>又由于<span class="math inline">\(a_1 + 1 = \sum_{i = 0}^{i &lt;
len}d_i \ p^i \geq \sum_{i \in S}d_{i} \ p^i \geq \sum_{i \in S}
26p^i\)</span>，所以<span class="math inline">\(\delta &lt; a_1 + 1 \leq
m\)</span>，所以<span class="math inline">\(h_2 = h_1 - a_1 - 1 - \delta
\geq h_1 - a_1 - m\)</span>，得证构造出的<span
class="math inline">\(h_2\)</span>符合要求，通过三次查询即可算出<span
class="math inline">\(p, m\)</span>的值。</p>
<h1 id="cf-round975-div.2">CF Round#975 Div.2</h1>
<p><a href="https://codeforces.com/contest/2019">Codeforces Round#975
Div2</a></p>
<h2 id="a.-max-plus-size">A. Max Plus Size</h2>
<p><code>题意</code>：给定<span
class="math inline">\(n\)</span>个数，可以选任意数染色，但相邻两个数不能同时染色，最大化染色数量+染色最大值之和。</p>
<p><code>题解</code>：最大值一定会取，取最大值最多带来奇偶性导致的1的损失，考虑该损失是否可以避免，即n是否为偶数或者最大值存在在奇数位置上。</p>
<h2 id="b.-all-pairs-segments">B. All Pairs Segments</h2>
<p><code>题意</code>：给定<span
class="math inline">\(n\)</span>个点，任意两点间都有一条线段，多次询问被覆盖<span
class="math inline">\(k\)</span>次的点有多少个。</p>
<p><code>题解</code>：按坐标顺序模拟一下即可，更新覆盖的线段数量，注意一下边界。</p>
<h2 id="c.-cards-partition">C. Cards Partition</h2>
<p><code>题意</code>：卡片上写有1到n的数字，每种卡片有若干张，可以额外购买至多k张任意数字卡片，然后需要将所有卡片等分为若干堆，保证堆内没有相同数字的卡片，求这个堆的最大大小。</p>
<p><code>题解</code>：堆的大小不会超过n，从大到小枚举判断，首先保证可以按此大小等分不剩余，在这个前提下显然买的多结果不会更差，只需要判断堆的数量是否小于最多种类的卡片数量即可。</p>
<h2 id="d.-speedbreaker">D. Speedbreaker</h2>
<p><code>题意</code>：有<span
class="math inline">\(n\)</span>个城市排成一列，时刻1可以从任意一个城市开始，每个时刻可以攻占已有城市相邻的任一城市。每个城市有一个时间要求<span
class="math inline">\(a_i\)</span>，求有多少个城市可以作为起始城市使得每个城市都在其时间要求前被攻占。</p>
<p><code>题解</code>：</p>
<p>想了一下暴力怎么取judge，觉得这个题我一定是思路想歪了，于是去看了一下题解。题解判断合法的方式是对于一个时刻<span
class="math inline">\(t\)</span>，所有时间要求小于等于<span
class="math inline">\(t\)</span>的城市形成的区间不能长于<span
class="math inline">\(t\)</span>，即不关注具体的攻占顺序而作出可行性的判断。</p>
<p>在这个基础上，起始城市相当于把某个城市的<span
class="math inline">\(a_i\)</span>置为1，对于一个时刻<span
class="math inline">\(t\)</span>对应的区间<span
class="math inline">\([l, r]\)</span>，选择城市<span
class="math inline">\(x\)</span>作为起始可能会导致区间发生变化，因此只需要枚举时间，考虑每个时刻对于<span
class="math inline">\(x\)</span>的约束求并即可。</p>
<h2 id="e.-tree-pruning">E. Tree Pruning</h2>
<p><code>题意</code>：给出一棵树，操作是可以去掉一个叶子结点，求最少多少次操作可以让所有叶子结点深度相同。</p>
<p><code>题解</code>：枚举最优解中叶子的深度，对于一个深度<span
class="math inline">\(d\)</span>，所有更深的点显然要被去掉，另外所有更浅的叶子也会带来一部分删除，这一部分对应的是最深子节点深度不到<span
class="math inline">\(d\)</span>的所有节点，分开统计即可。</p>
<h2 id="f.-max-plus-min-plus-size">F. Max Plus Min Plus Size</h2>
<p><code>题意</code>：给定<span
class="math inline">\(n\)</span>个数，可以选任意数染色，但相邻两个数不能同时染色，最大化染色数量+染色最大值+染色最小值之和。</p>
<p><code>题解</code>：借着第一题的思路，考虑枚举最小值，那么限制了所有更小的数不能选，转化为第一题的问题。从小到大枚举最小值，相当于不停地做区间割，在多个区间内完成A题，即维护是否存在区间可以在取最大值的情况下不损耗1即可。</p>
]]></content>
      <categories>
        <category>Codeforces</category>
      </categories>
      <tags>
        <tag>Algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>Keccak-256算法详解</title>
    <url>/posts/1b64dd1.html</url>
    <content><![CDATA[<h2 id="keccak-256">keccak-256</h2>
<p>keccak-256是以太坊采用的哈希加密函数，其与标准的SHA3-256仅仅在<code>padding</code>方式上有所不同，但是产生的结果是完全不一样的，我们主要关注以太坊使用的keccak-256。</p>
<p>keccak函数族采用了海绵结构，可以接受任意长度的输入，也可以提供任意长度的输出。keccak函数有两个用以描述其加密难度的参数：</p>
<ul>
<li>r：bitrate，表示加密时每一轮输入的数据比特数，决定了数据输入的长度</li>
<li>c：capacity，表示加密时数据的额外比特数，与参数r共同决定了哈希操作的数据长度<code>b=r+c</code></li>
</ul>
<p>keccak函数工作可以分为输入和输出两个主要的阶段，每个阶段都依赖于海绵结构，海绵结构中具有<code>b</code>比特的数据。（要求<span
class="math inline">\(b=25*2^{l}\)</span>，其中<span
class="math inline">\(l \in [0, 6]\)</span>）</p>
<p>在哈希函数工作前，首先按照比特率<code>r</code>对数据进行<code>padding</code>对齐，实现的方法为在数据后添加最少数量的<code>10*1</code>，使数据的长度为<code>r</code>的倍数，记得到的新数据<span
class="math inline">\(P =
M||pad[r]\)</span>。（标准的SHA3-256唯一不同的地方在于其修改了<code>padding</code>的方式，这导致了哈希结果巨大的差异）</p>
<p>在输入阶段，海绵结构在初始状态下是全0的<code>b</code>比特数据。依次向“海绵”中输入<code>r</code>比特的待加密数据，与海绵结构的前<code>r</code>比特进行异或，再通过加密函数<code>f()</code>得到新的海绵结构。不断重复上述过程直至对齐后的待加密数据全部输入海绵结构。</p>
<p>在输出过程中，依次从“海绵”中取出前<code>r</code>比特数据作为输出，如果长度小于要求的输出长度，则再通过加密函数<code>f()</code>得到新的海绵结构，重复取出前<code>r</code>比特数据直至长度大于等于要求。取得到的数据的前<code>n</code>位作为输出，即是<code>keccak-n</code>算法的哈希结果。</p>
<div style="width:70%;margin:auto">
<img src="/posts/1b64dd1/keccak.png" class="" title="keccak work process">
</div>
<p>接下来的重点是加密函数<code>f()</code>，这是<code>keccak</code>函数族更新海绵结构的核心，<code>f()</code>函数以<code>b</code>比特数据作为输入，以<code>b</code>比特数据作为输出。将<code>b</code>比特的数据排列为一个三维矩阵，将其中的一个元素记为<code>a[x][y][z]</code>，<span
class="math inline">\(x, y \in [0, 5), z \in
2^{l}\)</span>，特别地当<code>b=1600</code>时，可以将<code>a[x][y]</code>视为一个5x5的64位整型数组。<code>f()</code>函数会执行<code>n_&#123;r&#125;=12+2l</code>轮加密，每一轮加密由五个部分组成，接下来按照顺序依次介绍这五个部分。</p>
<p>需要补充的是以下的运算都定义在<code>GF(2)</code>上，加法即为异或操作，乘法即为与操作。</p>
<p><span class="math inline">\(\theta: a[x][y][z] = a[x][y][z] +
\sum_{y&#39;=0}^{4} a[x-1][y&#39;][z] +
\sum_{y&#39;=0}^{4}a[x+1][y&#39;][z-1]\)</span>。直观地理解<span
class="math inline">\(\theta\)</span>操作，将矩阵a的每一行与上一行，下一行进行异或操作，其中下一行需要进行循环左移一位。</p>
<p><span class="math inline">\(\rho: a[x][y][z] =
a[x][y][z-(t+1)(t+2)/2]\)</span>，其中<span class="math inline">\(t \in
[0, 24)\)</span>并且在<code>GF(5)</code>域中满足<span
class="math inline">\(\begin{pmatrix} 0 &amp; 1 \\ 2 &amp; 3
\end{pmatrix}^{t} \begin{pmatrix} 1 \\ 0 \end{pmatrix} = \begin{pmatrix}
x \\ y \end{pmatrix}\)</span>，特殊地当<span
class="math inline">\(x=y=0\)</span>时，令<span
class="math inline">\(t=-1\)</span>。<span
class="math inline">\(\rho\)</span>操作只对每个整形内部的比特顺序进行调整。</p>
<p><span class="math inline">\(\pi: a[x][y] = a[x&#39;][y&#39;],
\begin{pmatrix} x \\ y \end{pmatrix} = \begin{pmatrix} 0 &amp; 1 \\ 2
&amp; 3\end{pmatrix} \begin{pmatrix} x&#39; \\
y&#39;\end{pmatrix}\)</span>。 <span
class="math inline">\(\pi\)</span>操作只改变整形间的顺序。</p>
<p><span class="math inline">\(\chi: a[x] = a[x] + (a[x+1] +
1)a[x+2]\)</span>。<span
class="math inline">\(\chi\)</span>操作对行间进行操作，对数据进行杂凑加密。</p>
<p><span class="math inline">\(\iota: a = a + RC[i_{r}]\)</span>。<span
class="math inline">\(\iota\)</span>操作异或上轮常数进行混淆，其中轮常数只有64位，因此值改变了<code>a[0][0]</code>的值。</p>
<p>在实际实现中，可以通过预先计算好的常数对<span
class="math inline">\(\rho, \pi\)</span>操作优化加速。</p>
<h2 id="keccak-256算法的实现">keccak-256算法的实现</h2>
<h2 id="参考链接">参考链接</h2>
<ol type="1">
<li><a href="https://www.youtube.com/watch?v=ucw5ZW291V0">Learn
Keccak-256 on Youtube</a></li>
<li><a
href="https://keccak.team/files/Keccak-reference-3.0.pdf">Keccak-reference-3.0</a></li>
<li><a
href="https://keccak.team/files/Keccak-implementation-3.2.pdf">Keccak-implementation-3.2</a></li>
<li><a
href="https://keccak.team/keccak_specs_summary.html">Keccak-Official
website</a></li>
</ol>
]]></content>
      <categories>
        <category>Crypto</category>
      </categories>
      <tags>
        <tag>Ethereum</tag>
        <tag>Keccak-256</tag>
      </tags>
  </entry>
  <entry>
    <title>OpBench: A CPU Performance Benchmark for Ethereum Smart Contract Operation Code</title>
    <url>/posts/c7dc484a.html</url>
    <content><![CDATA[<p>最近正好在开发HardwareEVM，需要寻找一个测试集做性能测试。</p>
<h2 id="introduction">Introduction</h2>
<p>以太坊区块链的功能依靠矿工挖掘产生，而矿工执行智能合约获得对应的费用，如果执行需要的代价和获得奖励不匹配，则很容易被利用或者被选择性执行，造成对区块链网络的危害。因此本文对指令执行需要的资源以及获得的奖励进行了比较，提出了OpBench测试集，对所有的<strong>CPU敏感型指令</strong>进行了系统性的分析测试。</p>
<h2 id="design-of-opbench-system">Design of OpBench System</h2>
<p>OpBench的使用分为四个阶段：</p>
<ol type="1">
<li><p>通过PyEthApp来获取获取所有opcode的信息，包括参数数量，花费。</p></li>
<li><p>对于每一条opcode，生成一个对应的合约，其中重复调用该条指令，也包括必要的PUSH和POP指令以避免栈溢出。这个合约也会带有模拟的信息（如账户余额，时间戳，块信息）。一些指令的执行时间可能也受操作数的位宽影响（如ADD指令），对于此类指令，按照64,
128, 256位的情况分别生成测试。</p></li>
<li><p>将合约部署在测试链上并执行。</p></li>
<li><p>计算统计执行花费的时间，需要多次实验取均值，并得到数据的置信度。</p></li>
</ol>
<p>对于一部分Formula-based的指令，它们具有动态的开销，共有6条，分别对它们采取了不同的测试集。</p>
<ol type="1">
<li><p>EXP指令，开销与指数位宽有关，测试时根据指数位数不同，从64bit到256bit分别进行了测试。</p></li>
<li><p>SHA指令，开销与加密数据长有关，测试时选取1，2，3，4word分别测试。</p></li>
<li><p>SSTORE指令，开销与slot中的数据是否已经存在有关，测试时重复模拟了从0到非0，非0的修改。</p></li>
<li><p>CALLDATACOPY &amp;
CODECOPY指令，开销与拷贝的数据长有关，测试时选用了1，2word两种情况进行测试。</p></li>
<li><p>EXTCODECOPY指令，同类型4，但是选用了1，2，3word情况进行测试。</p></li>
</ol>
<p>在测试后还需要排除所使用的PUSH和POP语句对应的时间。</p>
<h2 id="result">Result</h2>
<p>在PyEthApp的测试中，指令重复执行100k次。而在Geth的测试中，所有指令重复执行1s，得到的结论如下：</p>
<ul>
<li><p>go客户端的性能更好。</p></li>
<li><p>客户端和系统共同影响着性能。</p></li>
<li><p>不同的指令单位CPU时间付出的费用是不同的。</p></li>
<li><p>python客户端在运行类指令执行更快，而go客户端在环境类指令更快。</p></li>
</ul>
]]></content>
      <categories>
        <category>Study</category>
      </categories>
      <tags>
        <tag>Ethereum</tag>
        <tag>TestBench</tag>
      </tags>
  </entry>
  <entry>
    <title>Risc Zero项目调研</title>
    <url>/posts/162bc2fd.html</url>
    <content><![CDATA[<p>最近老板提了一嘴 RISC
Zero，就去调研了一下这方面的内容，在这边总结一下，目前给我的感觉是中看不中用，去中心化的可验证计算还需要等待硬件算力的发展以及使用场景的更大规模出现，目前执行可验证计算仍需要专业的约束审计与高额的算力进行支撑，不过确实可以解决去中心化的问题。</p>
<h2 id="risc-zero">Risc Zero</h2>
<h3 id="introduction">Introduction</h3>
<p>Risc Zero
项目为零知识证明虚拟机提供了软件的解决方案，使得程序可以在虚拟机环境中可验证地执行，任何人都可以通过零知识证明的方式验证程序是否在虚拟机中被正确地执行。官方文档中对
Risc Zero 项目的解释为：<em>The RISC Zero zero-knowledge virtual machine
(zkVM) lets you prove correct execution of arbitrary Rust code.</em></p>
<p>具体到使用场景，用户将要执行的程序编译为 risc-v
指令集，送入零知识证明虚拟机（zkVM）中，虚拟机执行程序并生成公开的程序输出和证明，任何人可以通过执行程序的哈希值验证该程序是否在虚拟机中被正确地执行。那么对应地，Risc
Zero 可以按照功能划分为两个部分，一个是软件编写的 risc-v
模拟器，支持在执行的过程中生成
trace，另一个部分则负责零知识证明，用相应的密码学知识提供 trace
正确性的证明。</p>
<h4 id="verified-computation">Verified Computation</h4>
<p>零知识证明，即在验证者不知道具体信息的情况下，使验证者相信提供者的描述是真实的。零知识证明属于密码学的一个重要分支，目前已有许多密码学算法来实现零知识证明。那么回到
zkVM 的使用场景中，提供者持有的信息就是程序执行过程中收集的 execution
trace，尝试通过零知识证明的方式让验证者相信程序被正确地执行，并提供程序的输出。</p>
<div style="width:70%;margin:auto">
<img src="/posts/162bc2fd/flow.png" class="" title="nFET">
</div>
<p>zkVM
解决的问题是可验证计算，即程序不需要再次执行，通过简单地数学计算就可以验证第三方给出的执行结果。与这一概念很类似的是可信计算，基于零知识证明密码学体系的可验证计算也可以验证第三方执行的正确性，但是二者也有很大的不同。可验证计算的过程并不是要保护的机密，也就是说计算的程序，数据，过程，结果对于第三方计算服务提供商来说都是透明公开的，可验证计算只能验证该计算是被正确执行的。但是另一方面，可验证计算解决了去中心化的问题，可信计算无论如何都是基于硬件的
TPM
等机制，可信根一定包含硬件厂商颁发的签名证书，而可验证计算是不需要任何中心化结构的，任何人都可以对计算生成的证明进行验证。</p>
<h4 id="develop-friendly">Develop Friendly</h4>
<p>项目取名叫做 Risc Zero 是因为选取了 risc-v
作为虚拟机的指令集，模拟器中实现了 RV32-IM 指令集（未实现 CSR
以及特权态，但是官方画饼说正在开发相关功能），通过 ecall
指令来实现程序的退出，选择 risc-v 作为指令集的原因有：</p>
<ul>
<li>risc-v32 指令简洁规律，长度和操作寄存器都相对固定，便于生成
trace</li>
<li>risc-v 指令集支持模块化，更加灵活，便于扩展</li>
</ul>
<p>Risc Zero 项目使用 Rust 语言开发，基于 crates 和
riscv-toolchain，可以支持大多数的高级语言程序运行在 zkVM
中，只需要将它们通过工具链编译为 risc-v 二进制程序即可。</p>
<h3 id="detail">Detail</h3>
<h4 id="stark">STARK</h4>
<p>zk-STARK 全称 Zero-Knowledge Scalable Transparent Argument of
Knowledge（零知识可扩展透明知识论证），是一种零知识证明系统。与之对应的是
zk-SNARK 全称 Zero-Knowledge Succinct Non-interractive Argument of
Knowledge（零知识简洁非交互式知识论证），SNARK
更加高效，但是依赖于可信多方计算，这带来了额外的攻击面。 Risc Zero
目前使用的是 STARK 系统实现零知识证明。zkVM
问题中，项目尝试在不暴露执行过程的前提下，证明 execution trace
是正确的（正确性可由约束系统表示，例如在约束 <code>pc</code>
在没有跳转的情况下应当满足
<code>pc' = pc + 1</code>，例如<code>ADD</code>指令的结果应该满足<code>rd = rs + rt</code>），符合描述
risc-v 指令集的constraints（约束系统）的。</p>
<p>这里简单描述一下 zkVM
中零知识证明的过程，基于一部分科普博客得来，仅做一个形象的补充：每一条
trace
都相当于一个函数在某一点的取值（在本问题中，时间索引可以作为自变量，而信息本身则可以被编码为函数值），通过拉格朗日插值生成一个多项式函数，是最简单的验证想法。从提高安全性的角度考虑，为了避免在插值点过少时的知识泄露，以及一些碰撞攻击，要进行低度多项式扩展，基于随机生成的点提高多项式的次数，在一个更大的求值域上承诺多项式。</p>
<p>根据多项式基本定理，可以将约束条件转换为函数的次数判断，将所有约束转换为待验证的多项式，并通过添加随机多项式扩充为同一次数（同时保证约束条件成立时值仍为0），这样可以讲所有约束合并为一个多项式进行验证，计算其次数是否符合要求即可验证
trace 是否符合所有约束条件。从验证的便捷性考虑，将插值前的点做成一颗
merkle
tree，验证时由验证者随机选择一条树上的路径进行验证，因此在上一步扩展时通常会将散点先扩充为2的整数次幂。</p>
<p>这里仅仅是对于验证过程部分信息的描述，可以帮助理解零知识证明的过程，但是并不完全严谨与正确，仅供参考。Risc
Zero还使用了许多零知识证明算法或协议，这里列举一下名字，感兴趣的读者可以去学习相关的知识：PLONK,
FRI, DEEP-ALI, Fiat-Shamir, IOP....</p>
<h4 id="trace-contraints">Trace &amp; Contraints</h4>
<p>Trace 数据用于提供程序执行过程中的模拟器状态，根据 STARK
系统可以让验证者验证 trace 是否满足了约束系统，因此 trace
采集的数据必须完备，足以表明所有执行时的状态，任何没有追踪的必要信息都可能导致执行过程被篡改。Risc
Zero 项目中，采用了 rv32-im
作为模拟器的指令集，因此需要采集的数据可以分为两类：</p>
<ul>
<li><strong>CONTROL</strong>：全局执行状态（如开始，结束等虚拟机状态信息），待执行的二进制映像</li>
<li><strong>DATA</strong>：寄存器值，对内存的读写操作序列，模拟器微架构内的状态信息</li>
</ul>
<p>在模拟器中，每个时钟周期都会收集一次状态信息，根据时间顺序可以将其写作一个矩阵表格，每一行都是一个时钟周期内的状态信息，每一列则是同一种状态在不同周期的值。这里的时钟周期含义略异于硬件，采用了模拟器自定的周期，其中一条指令需要三个时钟周期来执行，通过这种划分可以保证在一个时钟周期内最多只会出现一个对内存的操作。</p>
<div style="width:70%;margin:auto">
<img src="/posts/162bc2fd/cycle.png" class="" title="nFET">
</div>
<p>那么只需要制作一套对上述数据进行约束的<strong>完备的</strong>约束系统，并通过
STARK 系统论证 trace
符合约束，即能说明用户传入的程序被正确地执行。这套约束系统需要完备地对
rv32-im
的执行做出约束，一旦其不能完整地约束指令语义，就会存在通过歧义的攻击方式，使得伪造的
trace
可以通过约束系统。这也是零知识证明约束系统存在的问题，需要对约束进行安全审计工作，目前尚无自动化工具进行测试分析工作。Risc
Zero
目前并没有开源，也并没有对其约束系统进行细致的说明和审计，安全性存疑。</p>
<p>通常来说大部分约束描述的是指令执行的语义信息，除此以外还有对内存操作的约束系统，在结束对程序的执行后，根据读写地址作为第一关键字，时间顺序作为第二关键字，排序所有对内存的操作，约束读取的值必须等于最后一次读取或写入的值，以保证执行过程中内存数据的完整性。另外，也是通过约束来保证用户的程序和输入被正确地加载到内存中。</p>
<h4 id="reciept">Reciept</h4>
<p>程序执行完毕后会返回公开的 reciept，包括两部分：</p>
<ul>
<li>Journal：公开的程序执行输出结果</li>
<li>Seal：公开的基于 STARK
系统的验证信息，任何验证者可以通过程序二进制映像的哈希值进行快速地验证</li>
</ul>
<p>因此 Risc Zero
并不能保护执行程序的隐私，执行的结果暴露给所有人，执行的过程暴露给了服务提供商，但是可以让用户转移程序执行的计算代价，仅承担简单的验证计算就可以得到正确的执行结果。目前一个应用是
Ethereum
Layer2，用户的合约在次级链上可验证执行，将结果打包发往主链，以减少用户的计算开销。</p>
<h4 id="host-interacion">Host Interacion</h4>
<p>Risc Zero
目前还无法动态地与外界进行通信，团队声称正在开发特权态相关的功能以实现该功能。如果仅仅是与用户进行静态地数据交互，可以通过未初始化内存区域进行通信，这可以绕过上文提到的内存约束系统。</p>
<h4 id="performace">Performace</h4>
<p>官网未对性能进行详细说明，目测是表现很糟糕，找到了一个非官方的性能测试实验，使用
Risc Zero 重新执行了区块17735424生成证明，在 AWS 上租用64块
GPU，花了十分钟。</p>
<h3 id="special-technology">Special Technology</h3>
<h4 id="continuations">Continuations</h4>
<p>zkVM 的解决方案中一直存在着一个问题：基于 merkle tree 的结构，需要将
trace
随机生成补全至2的整数次幂，但是又受限于有限域的选择不能太大，因此对程序执行的时钟周期数有上限的限制，另外较大的
trace 会使得验证的生成复杂度快速增长。为了解决这个问题， Risc Zero
提出了 Continuations
机制，将程序划分为若干段（文档中并没有介绍划分的细节，依据），每一段（session）独立地执行，证明，具有依赖关系的段需要保证段开始时的状态与上一段结束时的状态一致。通过延续机制，可以实现：</p>
<ul>
<li>暂停：当程序需要暂停时，视为一个段的结束，等待继续时开始下一段的执行，借此也可以实现热启动</li>
<li>并行化：复杂的程序可以分为多个段，交由分布式的节点进行计算，每个段间的证明可以独立生成</li>
<li>不受限制：程序可以是任意复杂的，划分为若干不超限制的段执行</li>
</ul>
<h4 id="bonsai">Bonsai</h4>
<p>Risc Zero
项目的野心很大，虽然目前看来只有区块链这边很支持零知识证明，但是在未来这是可能成为去中心化世界的基础设施的，作为一个
zkVM 项目，它也提供了 verified computation
的支持，Bonsai就是云可验证计算平台，并且已经投入试验运行。</p>
<div style="width:70%;margin:auto">
<img src="/posts/162bc2fd/bonsai.png" class="" title="nFET">
</div>
<p>参考链接：</p>
<ol type="1">
<li><a
href="https://blog.csdn.net/mutourend/article/details/134001186">Zeth：首个Type
0 zkEVM</a></li>
<li><a
href="https://trapdoor-tech.github.io/zkstark-book/">深入理解zk-STARK证明系统</a></li>
</ol>
]]></content>
      <categories>
        <category>Study</category>
      </categories>
      <tags>
        <tag>Ethereum</tag>
        <tag>Crypto</tag>
        <tag>Zero knowledge proof</tag>
        <tag>Virtual machine</tag>
      </tags>
  </entry>
  <entry>
    <title>Scalable Memory Protection in the PENGLAI Enclave</title>
    <url>/posts/ea6bfb20.html</url>
    <content><![CDATA[<p>蓬莱是上交研究的RISC-V架构下的可信执行环境方案。</p>
<h2 id="introduction">Introduction</h2>
<p>目前已经有许多成熟的TEE方案，如Intel SGX，AMD SEV，ARM
TrustZone，以及一些学术界的研究工作，但是过去的TEE方案都存在着一些缺陷，较明显的就是无法适应云端应用的特性：弹性的内存分配，高资源负载，auto-scaling（动态调整实例资源分配），短执行时间等。这往往是受限于方案设计的内存保护机制：</p>
<ol type="1">
<li>不可扩展的内存划分/隔离：大多数设计使用了硬件中单独为飞地存在的内存，具有硬件上的大小和数量限制</li>
<li>不可扩展的内存完整性保护：使用传统的Merkel Hash
Tree方案校验完整性是难以扩展的，只能对固定大小的内存核验完整性</li>
<li>不可扩展的内存安全初始化：初始化一片飞地的内存往往需要数秒，但是服务本身的运行时间通常很短（&lt;1s）</li>
</ol>
<p>因此本篇文章提出的蓬莱设计就是要解决上述问题，在安全内存的大小和粒度、飞地的数量、启动飞地的延迟方面提供支持。</p>
<h2 id="motivation">Motivation</h2>
<p>目前的解决方案都存在着各自的缺陷，在可创建的飞地数量，可用的内存大小，启动飞地的速度，内存完整性保护的粒度，保护的额外开销，可信启动的证明能力等方面上表现出不同层级的限制。</p>
<div style="width:80%;margin:auto">

</div>
<p>蓬莱的目标就是解决上述已知的缺陷，提供可扩展的内存管理，支持不限量的飞地同时运行，以及可扩展的内存大小，在不添加过大开销的基础上，提供应对更高权限软件攻击，片外硬件攻击，基于缓存的侧信道攻击的安全支持。</p>
<h2 id="system-overview">System Overview</h2>
<h3 id="architecture">Architecture</h3>
<p>蓬莱是一个软硬件协同的方案，软件实现了Secure
Monitor用于管理并监视安全世界，硬件补充了下图中红色的新单元，以实现预期的功能目标。类似于TrustZone的想法，蓬莱也将系统状态划分为安全世界和不安全世界，分别运行不同的操作系统和软件，其中Secure
Monitor运行于最高权限级。</p>
<div style="width:80%;margin:auto">

</div>
<h3 id="thread-model">Thread Model</h3>
<p>蓬莱的可信基为CPU和Monitor软件正确性，Monitor通过安全启动的方式由ROM中加载，在加载完成后会通过RISC-V自身的内存安全机制（PMP）保护自身。</p>
<p>在蓬莱方案中，威胁模型有以下三个攻击面：</p>
<ol type="1">
<li>权限级软件攻击：攻击者可以完全地控制非安全世界的OS和软件</li>
<li>物理硬件攻击：攻击者可以截获并分析CPU与其他硬件（如内存）的通信信息</li>
<li>侧信道攻击：考虑控制信道攻击和基于缓存的侧信道攻击</li>
</ol>
<p>这里补充一下侧信道攻击，以SGX技术为例，CPU通过指令切换状态，分别对应飞地内的程序和飞地外的世界，在这个过程中两个世界会共享许多硬件资源，这就导致了信息泄露的可行性。控制信道攻击一般指对页表信息的泄露利用，SGX方案中非安全世界试图访问安全世界内存时会发生Page
Fault，因此可以以页为单位对安全世界进行探查，更进一步还可以利用页表的信息位，观察安全世界的逻辑行为信息。除此以外，流水线，分支预测器，缓存都可以通过侧信道攻击的方式泄露出安全世界的信息。</p>
<p>在本篇文章中不考虑其他的侧信道攻击，与本方案关注的内存安全无关，也不考虑Dos攻击。</p>
<h2 id="design">Design</h2>
<h3 id="fine-grained-flexible-memory-isolation">Fine-grained Flexible
Memory Isolation</h3>
<p>为了实现细粒度的内存隔离，蓬莱像许多方案一样维护了表明物理页表所属世界的bitmap，以页表为粒度进行内存隔离。传统的方案往往基于此，在进行虚拟地址转换时，进行两次查找，先查询bitmap确认所查页表是否属于安全世界，再进行二次读取，这导致了25.2%的额外开销。</p>
<p>而蓬莱方案则关注在<strong>页表的管理</strong>，观察到mapping的操作远小于访问内存的次数，蓬莱在硬件中添加了Guarded
Page
Table模块对页表进行管理，并尝试通过页表的管理避免二次读取，以降低额外的性能开销。</p>
<p>在Guarded
PT模块中，引入了两个新的寄存器<code>reg_hptarea_start</code>和<code>reg_hptarea_size</code>，HPT表示Host
Page
Table即不信任的主机的页表区域，通过这两个寄存器将主机可以使用的页表全部限制在一段连续的物理空间内。</p>
<p>当主机试图更改页表区域时，会触发trap交由secure
monitor进行检查，通过secure
page的bitmap对映射区域的属性进行检查，在非安全世界中只能映射到non-secure
page，而在安全世界中则不受限制，可以对两个世界的内存进行操作。</p>
<div style="width:80%;margin:auto">
<img src="/posts/ea6bfb20/GPT.png" class="" title="Guarded Page Table">
</div>
<p>回顾TEE的目标，我们需要禁止非安全世界访问安全世界，结合下图所示的MMU扩展，在虚拟地址转换的每一步过程中都将得到的下一级页表地址进行检查，是否处于<code>hptarea</code>寄存器规定的范围中。</p>
<p>通过MMU，我们限定了非安全世界访问的页表范围只能处于<code>hptarea</code>中，而在这一范围内页表的所有改动都将触发trap，由monitor进行bitmap核验后写入，因此限制了非安全世界对安全世界的访问。</p>
<div style="width:80%;margin:auto">
<img src="/posts/ea6bfb20/MMU.png" class="" title="MMU">
</div>
<p><strong>因此，总结来看这一方案创新性地将安全检查的代价由访问内存时转移到了创建映射时，基于映射次数较少的观察，该方案可以显著地降低访问开销。</strong></p>
<p>当然，这一方案还需要考虑TLB的存在，由于硬件中缺少直接作用于TLB的指令，TLB难以被直接修改，另外在切换到安全世界的过程中，TLB也会被刷新，因此不存在非安全世界直接通过TLB访问安全世界的可能。</p>
<p>总结来看，这一方案对硬件的修改较小（加入了bitmap和MMU的逻辑判断），唯一需求的物理连续区域是相对较少的页标区域，实现了页级别的内存隔离并减少了二次查找的访问开销。</p>
<h3 id="scalable-memory-integrity-protection">Scalable Memory Integrity
Protection</h3>
<p>为了保护安全世界的内存完整性，过去方案采用了Merkle
Tree的方式对连续内存进行哈希，形成树形结构并通过根节点的哈希值保护整棵树的完整性。这些方案普遍存在一些问题，如保护的是一段连续的特定地址内存，树节点存储代价很大，更新哈希值开销较大等。</p>
<p>蓬莱提出了硬件上的Mountable Merkle
Tree对安全世界的内存进行完整性保护。MMT最基本的单位是SubTree，不同层级的Merkle
Tree共同通过哈希对整片内存进行完整性保护，最下层的叶子节点对应4个1MB的页。根据每一级子节点数量的设计，MMT最多可以对512GB内存进行完整性验证。</p>
<p>为了防止Merkle
Tree中的数值不被篡改，蓬莱建立了依托于SoC的信任链，从可信的SoC出发，将树的根节点地址存于SoC中的<code>Root-root</code>寄存器中，保证根不受篡改，依据根节点的哈希值递归保证子节点的哈希值不受篡改。</p>
<div style="width:80%;margin:auto">
<img src="/posts/ea6bfb20/flow.png" class="" title="Protect Flow">
</div>
<p>这里使用的哈希树是Counter-based Integrity
Tree，这里没有详细介绍，我看了Vault那篇文章才理解这个词的含义，具体的内容可以在Vault论文阅读摘要中查看。相比于传统的BMT，蓬莱方案中添加了两个域，<code>idx</code>和<code>extra</code>，这是为了减少对同一分支的修改多次触发<code>global counter</code>域的更新，<code>idx</code>指向刚刚更改的<code>local counter</code>，而<code>extra</code>则作为高位一同作为计数器，当<code>extra</code>域也溢出或<code>idx</code>更换时再更新<code>global</code>域，以此减少路径哈希更新的频率。</p>
<div style="width:80%;margin:auto">
<img src="/posts/ea6bfb20/MMT.png" class="" title="Counter-based Integrity Tree">
</div>
<p>上图为完整性信任链的建立，<code>Root-root</code>存储在SoC中，认为是可信任的，所有子节点的地址信息和计数器信息都存储在一块称为MMT
meta-zone的区域中，完整性由root保护。这片区域的信息又保护了counter指向的Subtree
Node区域，存储了所需的数据信息，通过哈希值对Secure
Mem区域进行完整性的保护。</p>
<div style="width:80%;margin:auto">

</div>
<p>论文中提供了这样一幅图来表述Mountable的概念，Merkle
Tree保护的是覆盖区域内所有内存的完整性，因此可挂载并不是指改变树的结构，而是可挂载到<strong>SoC</strong>上的意思。</p>
<p>所有对完整性的验证从某一叶子节点出发，到达某一个在SoC中存储（也就是挂载）的节点时停止。SoC中留有的硬件空间最多可以存储32个子节点信息，存满后就依据LRU进行换入换出，利用空间访问的一致性来减少数据更新的代价，在一次读写后可以访问更少数量的节点来校验完整性。<strong>相比于传统的MT，MMT不将一整条从根节点出发的链存储在SoC上，而是存储子树的低层级节点，提高了SoC空间的利用率</strong>。</p>
<p>在系统启动时，MMT
meta-zone是一块可配置的地址空间，由bootROM进行初始配置并通过硬件访问权限设置进行保护。</p>
<p>考虑到安全世界和非安全世界存在着必要的交换，蓬莱还设置了hole
region的概念，这是一个可配置的连续区域，由寄存器<code>reg_hole_va_start</code>和<code>reg_hole_va_size</code>配置。在hole区域中，执行相反的完整性验证，即当<code>reg_ms=ENCL_MODE</code>时不对hole区域做完整性校验，提供二者共享的内存。这需要在MMT中做出支持，不对代表hole区域的子树进行完整性校验。</p>
<h3 id="secure-memory-initialization-with-shadow-fork">Secure Memory
Initialization with Shadow Fork</h3>
<p>建立飞地的代价是必须要考虑的因素，为了减小初始化的开销，蓬莱借鉴了shadow-fork的思路。考虑在云服务场景，先建立一个具有必须的运行时环境的shadow-enclave，并进行哈希值证明，当建立飞地时，通过fork的思路，共享R和RX段，只拷贝W段，初始化栈。</p>
<p>初始化飞地比较大的代价其实是在验证阶段，需要对内存进行完整性的证明，而根据影子飞地，可以轻松地使用已经计算出的证明，证明启动的飞地是可行的，再基于此进行进一步的保护即可。</p>
<h3 id="on-demand-cache-line-locking">On-demand Cache Line Locking</h3>
<p>蓬莱设计也考虑了针对缓存的侧信道攻击，提供了<code>CACHE_LINE_LOCK</code>和<code>CACHE_LINE_UNLOCK</code>原语，对cache
line硬件层上锁，使得内存读写时间一致。这不可避免地会降低性能，因此蓬莱建议只在加密等较高安全需求的部分使用。在不同的飞地间切换时，cache的锁会作为上下文随之切换。</p>
<h2 id="implementation">Implementation</h2>
<h3 id="hardware-implementation">Hardware Implementation</h3>
<p>蓬莱把新增加的寄存器做成了CSR，扩展了MMU的Page Table
Walker，增加了数据的合法性验证和HPT核验。在核外，蓬莱扩展了Memory
Controller以实现MMT功能，增加了Mount Table和Root-root，Mount
Table结构与Cache类似，同样使用LRU策略进行换入换出。</p>
<div style="width:80%;margin:auto">
<img src="/posts/ea6bfb20/layout.png" class="" title="Layout">
</div>
<h3 id="software-implementation">Software Implementation</h3>
<p>蓬莱在OpenSBI和Berkeley Boot Loader上都做了Secure
Monitor的实现，它包含了飞地管理，硬件扩展部分管理，内存检查，以及一些基础的库支持，仅需要6399行代码。</p>
<p>蓬莱同时也对Linux
Kernel做了改动，主要是为了适配HPT的设计，分配HPT区域，劫持对于页表的修改行为，转交monitor进行判断。</p>
<p>飞地的类型包括普通飞地（用以创建可信执行环境），影子飞地（用以快速创建飞地），服务飞地。服务飞地并不能单独存在，需要从其他飞地继承时间片，用以构建飞地链，服务飞地可以提供部分OS的服务，如文件系统等。</p>
<p>蓬莱的形式化验证工作正在进行中。</p>
<h2 id="evaluation">Evaluation</h2>
<p>蓬莱在VC707开发板上基于SiFive Freedom
U500实现，采用了SPECCPU,Redis,RV8,Coremark测试集，与Linux（作为基准）,Keystone进行整体的性能对比。也在GEM5上进行模拟实验，比较MMT和SIT，VAULT的完整性保护方案代价。</p>
<p>通过实验结果得到的结论整理如下：</p>
<ul>
<li>GPT会在mapping阶段产生26%~46%的额外开销，在内存访问阶段基本不会产生影响</li>
<li>Shadow-fork可以大量节省启动飞地的时间开销</li>
<li>MMT在随机内存访问测试中表现出色，在较大访问范围测试中由于mount机制，不会产生过大的开销。在随机访问测试中，mount操作只占用了10%的总访问时间，而完整性验证占了20%。</li>
<li>Cache Locking会产生17.73%的额外开销</li>
<li>对硬件的额外改动基本不需要占用硬件资源</li>
</ul>
<h2 id="discussion">Discussion</h2>
<p>实现蓬莱方案，需要两点支持：指令集需要支持比OS和hypervisor更高层级的特权级支持（现代指令集都支持），CPU需要有MMU模块以支持不同粒度内存管理（现代CPU中基本也存在）。</p>
<p>针对内存管理的侧信道攻击讨论：</p>
<ul>
<li>OS无法直接操作页表，避免了对页表观测的攻击</li>
<li>Cache可以选择性地加锁，避免了对缓存时间的攻击</li>
</ul>
]]></content>
      <categories>
        <category>Paper Note</category>
      </categories>
      <tags>
        <tag>TEE</tag>
        <tag>Risc-V</tag>
      </tags>
  </entry>
  <entry>
    <title>VAULT:Reducing Paging Overheads in SGX with Efficient Integrity Verification Structures</title>
    <url>/posts/7870c3f8.html</url>
    <content><![CDATA[<p>VAULT是基于SGX的改进工作，主要关注于隔离内存产生的额外开销。</p>
<h2 id="introduction">Introduction</h2>
<p>SGX是Intel提出的TEE解决方案，可以保护敏感信息的CIA（Confidentiality,
Integrity,
Authentication），在内存保护方面，SGX可以保护敏感页内存的安全性。SGX讲内存划分为两个区域，EPC（Enclave
Page
Cache）区域存放敏感页内存数据，实施保护机制，而以外的non-EPC区域不做保护。SGX方案需要硬件上提供EPC区域，这是一段片上的内存区域，具有物理大小的限制，论文中提及是128MB。</p>
<p>那么SGX针对内存的保护主要会产生两种额外开销，时间开销和空间开销。时间开销分为两个方面，一方面是访问敏感内存的时候需要经过校验数据完整性并解密使用。另一方面当敏感数据占满了EPC区域后，需要将敏感信息加密后存储到非EPC区域，SGX在触发EPC
miss（需要的内容不在EPC中，包含需要换入换出的情况）时通过page
fault的方式进行替换，这带来了上下文切换，数据拷贝，加解密的额外时间开销。空间开销主要是需要对数据进行保护，需要存储额外的信息。</p>
<p>本文工作目标就是降低这两种额外开销。</p>
<h2 id="background">BackGround</h2>
<h3 id="threat-model">Threat Model</h3>
<p>在TEE方案中，一般来说除了硬件本身以外都不属于可信基，应用层软件可能是攻击者运行，操作系统可能被攻击者操纵，甚至设备实体也可以被攻击者物理接触。在这种威胁模型下，要保证敏感信息的安全性非常困难，只考虑内存的安全性，通常我们认为片内的内存是不易被攻击者获取的，相对安全，防护的重点在于从片外内存中的数据。</p>
<p>常见的针对内存数据的攻击有：</p>
<ul>
<li><p>攻击者通过恶意的操作系统或软件窃取不具备访问权限的敏感信息</p></li>
<li><p>攻击者通过物理手段读取甚至修改片外内存中的数据（TEE方案通常支持多个飞地的建立，当EPC内存不足以存储所有敏感信息时就会发生换入换出，再次读取换出内容时就不能再信任内存提供的数据）</p></li>
</ul>
<p>这要求TEE方案做好严格的权限控制，确保敏感信息不被泄露，同时又不能信任内存提供的数据。SGX中在EPC中划分出了区域存储页的元数据，来存储访问页的权限信息，实现了权限控制。比较难的问题是如何信任内存中保存的数据。</p>
<h3 id="integrity">Integrity</h3>
<p>完整性的保护是TEE内存方案中最为棘手的部分，从原理上看我们需要在有限的可信安全空间内（如上文提到的128MBEPC）对庞大的内存空间做完整性保护，保护其内容不受篡改，由于片上的空间一定更少，一定存储的是特征信息（即不可能做到非概率意义上的安全保证），通过运算验证，以时间代价换取保护。</p>
<p>最基本的想法是利用MAC（Message Authentication
Code）来对不可信内存中提供的数据进行校验。如果我们能对每个数据块（这里指一个基本的数据单元）生成一个MAC，同时利用片上产生的可信的私钥进行加密，存储到不可信内存中，待到读取数据时先进行解密，同时校验MAC，就可以确保数据的完整性。（这里MAC是存储在片上的可信数据，存储在不可信内存中则需要考虑存放的位置，以及MAC的保护问题）</p>
<h4 id="merkle-tree">Merkle Tree</h4>
<p>只使用MAC是不够的，这会带来重放攻击的风险，即攻击者返回其他未经篡改的区块数据，仍然可以通过MAC校验，但是数据的正确性无法得到保障。Merkle
Tree的思路是对所有的数据建树，叶子节点为数据块的MAC，非叶子节点的值是所有子节点的哈希校验值，这样就可以通过根哈希值保护整棵树所覆盖地址范围的完整性。而只需要把根哈希值，或者前两层的节点哈希值储存在片内，就可以校验数据不受篡改。在校验时，各层级可以并行计算，同时验证从根出发的一条路径上的所有哈希值是否正确。</p>
<p>Merkle
Tree的问题是维护的代价比较大，出于性能考虑一个节点存储的数据最好是一个cache
line大小，举例来说，在64B的cache
line中只能存储8个64位的校验值，因此一个非叶子节点最多有8个子节点，若是要维护16GB地址范围的数据完整性，则需要10层树深，这带来了大量的空间开销和访问带宽开销（需要并行访问11个地址的数据）。另外，每一个对数据的写入操作都会导致一条到根的路径的哈希值更新，也会成为性能的瓶颈。</p>
<h4 id="bonsai-merkle-tree">Bonsai Merkle Tree</h4>
<p>解决重放攻击最直观的思路是nounce，即增加一个计数器，以抵御重放攻击，考虑对每个数据块维护一个计数器记录写入修改的次数，在换出加密时将计数器作为密钥的一部分，这样在验证时就可以证明是该数据块的最新数据，避免重放攻击。因此Bonsai
Merkle Tree在Merkle
Tree的基础上加以改进，将要校验的数据从哈希值改为了计数器，这样可以增加子节点的数量，因为计数器具有更少的位数，从而在同样保护范围内降低树深，减少开销。</p>
<p>仍以64B举例，每个节点可以包含64个8位计数器，即可以有64个子节点。这时面临的问题是性能和安全性的取舍，如果计数器的位数较少，那么当修改次数超过了<span
class="math inline">\(2^{8}\)</span>后counter就会循环，产生重放攻击的风险，但是越多的子节点数量就意味着更小的开销。</p>
<p>进一步地，还可以通过global
counter来进行优化，改为64个7位计数器和一个64位的全局计数器，可以理解为采用了64个71(7+64)位的计数器以保证安全性，同时这64个计数器共用了全局计数器作为公共的前缀，每当一个计数器溢出时便更改全局计数器，以此同时提高了安全性和性能，71位的计数器已经可以应对绝大多数的飞地应用场景生命周期了。</p>
<h3 id="sgx-baseline">SGX baseline</h3>
<p>Intel
SGX是现行的工业界实现，在硬件上拥有128MB的片上安全空间，其中的32MB用来存储metadata。当安全空间用完需要换入换出时，会以页为单位将换出的数据归入一个non-EPC区域的完整性树管理，以保证对非安全区域的数据篡改会被发现，non-EPC区域的完整性树可以通过根哈希等方式由安全区域保护。SGX通过维护ELRANGE来确定EPC区域的范围。</p>
<p>SGX也实现了对访问安全数据的权限控制，维护了数据结构EPCM(Enclave Page
Cache
Map)，在数据页被分配时记录了该页对应的虚拟地址，所属的飞地以及相应的权限。考虑到操作系统可能是被攻击的，具有操作TLB的权限，在TLB将虚拟地址翻译为物理地址后，处理器发现该物理地址对应EPC区域后，需要用物理地址去查找相应的EPCM表项，检查所访问的虚拟地址是否与预先记录的匹配，是否属于同一飞地，进行权限校验。</p>
<p>在SGX中，如果飞地访问的页不在EPC区域中，会触发缺页异常，退出飞地转交给OS将缺页拷贝到EPC中（还有可能的换入换出操作），这带来了较大的访问开销。</p>
<p>SGX使用SGX Integrity Tree(SIT)来维护数据的完整性，类似于Bonsai Merkle
Tree也采用了counter的形式，每个block采用56bit的counter记录（无global
counter），每个节点包含8个counter和64bit哈希（实际为56位哈希），哈希值由节点自身的8个counter和父节点对应的counter共同计算以在父子节点间建立联系，根节点的counter保护在片内。</p>
<h2 id="proposed-techniques">Proposed Techniques</h2>
<h3 id="unifying-the-epc-and-non-epc-regions">Unifying the EPC and
non-EPC Regions</h3>
<p>本文提出不再像SGX一样区分安全区域与非安全区域，即对全部的地址维护EPCM。在访问内存时通过物理地址查找到对应的表项，如果不是SENSITVE数据则正常读写，如果是SENSITIVE数据，类似地通过表项比对虚拟地址的映射，飞地的权限和所属，在通过检查后允许访问。</p>
<p>这一设想简化了EPC的机制设计，在地址转换过程中无需比较EPC范围寄存器。但是代价是EPCM表的大小增加，对于每一个页，都需要存储映射的虚拟地址，以及权限信息，以16Byte计算，需要<span
class="math inline">\(\frac{16B}{4KB}=0.04\%\)</span>的额外地址空间。类似于TLB表，EPCM表在这一机制下也需要参与到每一次地址转换过程中，可以采用类似的缓存机制进行加速。</p>
<h3 id="variable-arity-unified-enctypted-leaftreevault">Variable Arity
Unified enctypted-LeafTree(VAULT)</h3>
<p>VAUT(Variable Arity Unified Tree)在Bonsai Merkle
Tree的基础上做了进一步的优化。在Bonsai结构中，一次写入过程中会从叶节点出发向上更改计数器，直到命中了片上缓存的节点停止，那么显然层级越高的节点counter改变的越频繁，每当一个local
counter溢出时，需要将节点的全部counter置为0，对应的就是需要重新计算所有子节点的MAC值。</p>
<p>基于此，VAUT允许每一层的counter位宽不同，越高的层级counter宽度越长。仍然按照一个节点采取一个64B
cache
line的大小进行设计，每个节点固定拥有一个64bit-HASH用来保护一致性，64bit
global
counter记录溢出次数，还剩下448bit可控使用。在叶子节点，分配为64个6bit-counter，在倒数第二层分配为32个12bit-counter，再上层为16个24bit-counter。</p>
<div style="width:80%;margin:auto">
<img src="/posts/7870c3f8/VAULT.png" class="" title="VAUT">
</div>
<p>相比于BMT，在非叶子节点也采用了计数器，相比于SIT，可变的counter长度以及global
counter使用让VAUT在使用同样大小空间保护更大的安全区域，并且具有更小的刷新代价。以64GB地址空间为例，采用BMT需要9层树深，SIT需要10层树深，而VAUT只需要7层，需要的时间代价和空间代价都小于二者。</p>
<p>VAUT看着很美好，同样的空间内容纳更多的counter并且也减少了刷新带来的重加密开销，但是代价是作为叶子节点，每个block只能分到6bit的计数器，会导致叶节点的刷新次数增大，这是不可避免的问题，计数器更宽对应更少的刷新和更小的覆盖面积。为了缓解这一问题，VAULT在VAUT的基础上引入了对叶子结点的加密，即对叶子节点采用父节点对应的64+12bit计数器作为密钥加密，从而省去叶子结点的HASH值建立其父子节点间的关系，省去的HASH值平分到计数器的宽度上，变为7bit计数器，减少了一倍的溢出可能。但是显然这会带来额外的时间开销，访问叶子节点数据还需要先访问父节点的数据，因此这一方法本质上是在<strong>牺牲动态时间换静态时间</strong>。</p>
<div style="width:80%;margin:auto">
<img src="/posts/7870c3f8/VAULT.png" class="" title="VAUT">
</div>
<p>VAULT的改动整体是围绕着counter的宽度和数量进行设计的，作为宽度的取舍，VAULT可以轻易地覆盖较大范围的地址空间，但是会有相对更多的计数器溢出刷新次数（变宽计数器设计只能一定程度上缓解这一问题），而叶子结点的加密也引入了访问内存时新的关键路径。</p>
<p>如果仅仅看这一设计，保护同样大小空间的空间代价和时间代价肯定都是更小的，层数更少带来较大的优势，但是同样需要考虑减少计数器宽度对刷新代价造成的影响，具体的提升还需要实验去验证。也正是因为VAULT的优势是保护更大的空间，才有上一节对于EPC空间概念泛化的讨论。</p>
<h3 id="mac-allocation">MAC Allocation</h3>
<p>最后一个问题，既然VAULT泛化了全部的内存空间为安全空间，那么对应的MAC大小将会暴增，如何存储MAC校验值成为了新的问题。文中提出了两个改进方案。</p>
<p>第一点是Shared MAC with
Compression(SMC)，对于数据块进行压缩，将MAC存进里面，我个人觉得这是有些强行的，首先块不一定能压缩，虽然大部分块是可以压缩的（这里提到了Base-Delta-Immediate，看了一下大部分内存中存在许多没有被利用的前导0，就可以进行压缩），其次这引入了额外的解码代价，对压缩数据还原可能会成为关键路径。另一方面SMC也提出了共享MAC的思路，让多个块共享同一MAC，这也是用时间换空间的替代方案，校验时需要将多个数据块一同取出，如果利用上空间一致性可能不会带来过大的overhead。</p>
<p>第二点是On-Demand MAC
Allocation(ODMA)，也就是按需分配MAC，不敏感不关键的数据就不要产生MAC，聊胜于无的补充。</p>
]]></content>
      <categories>
        <category>Paper Note</category>
      </categories>
      <tags>
        <tag>TEE</tag>
        <tag>SGX</tag>
      </tags>
  </entry>
  <entry>
    <title>Why and How zk-SNARK Works: Definitive Explanation</title>
    <url>/posts/d66984b9.html</url>
    <content><![CDATA[<p>零知识证明方案是目前的以太坊扩容主流解决方案，因此通过这篇论文学习零知识证明的数学原理。</p>
<h2 id="零知识证明算法概览">零知识证明算法概览</h2>
<p>零知识证明是指是一方（证明者）向另一方（检验者）证明某命题的方法，特点是过程中除“该命题为真”之事外，不泄露任何信息。在零知识证明系统中，有Prover,
Verifier两个角色，针对需要验证的知识，生成对应的Circuit（逻辑电路），这里的Circuit指的是知识需要符合的逻辑（亦或称之为待证明的知识本身），即一个逻辑系统，使用数学语言的方式进行描述，而并非指代数字电路。在以太坊目前的L2扩容方案中，链下节点作为证明者，通过零知识证明方案证明L2中合约被正确执行，因此L1网络可以在校验的基础上不经过再次执行，直接采纳L2的执行结果提交至L1网络。</p>
<p>零知识证明目前有多种不同的实现方案，依赖于不同的密码学问题，也有不同的实现方法，其中比较常用的一类叫做ZK-SNARK（Zero-Knowledge
Succinct Non-Interactive Argument of
Knowledge）。其中Succinct指简洁，即验证过程的复杂度要为知识本身的sublinear级别，Non-Interactive指非交互，验证者也证明者无需进行多次通信完成验证，任何符合这两点要求且可以对一定形式的知识进行证明的系统都可以称为ZK-SNARK。基于简洁性，ZK-SNARK在大规模数据的证明上表现良好，验证者可以在极短的时间内对极短的证明进行验证，这也是被L2广泛应用的证明系统。</p>
<p>我调研了一些零知识证明系统，列举了其中不同的四种方法：</p>
<ul>
<li><p>Groth：提出于2016年，属于ZK-SNARK，它的验证代价极小，但需要对每一个不同的Circuit重新进行setup，即向证明者和验证者约定一系列证明使用的参数</p></li>
<li><p>ZKG：通过bilinear
mapping和多项式承诺来完成零知识证明，它可以对不同的Circuit使用同一套参数进行验证，并且验证时间非常短，但setup过程中需要进行可信初始化，即参数的生成需要至少一个信任方在参与产生后销毁（多方可信计算）</p></li>
<li><p>BulletProof：通过离散对数问题和多项式承诺来完成零知识证明，它的验证需要相对更大的代价，但是较于ZKG的优点是不需要可信初始化。</p></li>
<li><p>ZK-STARK：也有人称其为SNARK的一种，不需要可信初始化，验证随数据规模变化较严重，适合于小数据的快速验证。</p></li>
</ul>
<p>本文主要参考了标题同名文章，其中重点介绍了KZG系统在零知识证明中的原理和使用。</p>
<h2 id="kzg多项式承诺系统">KZG多项式承诺系统</h2>
<p>让我们假设一个场景，Prover想向Verifier证明他知道一个多项式形式的知识（自然Verifier也需要知道该多项式），这个待证明的多项式记为<span
class="math inline">\(t(x)\)</span>，而证明者为了不泄漏<span
class="math inline">\(t(x)\)</span>，则构造一个多项式<span
class="math inline">\(p(x)\)</span>，使得<span
class="math inline">\(h(x) =
\frac{p(x)}{t(x)}\)</span>，即可以整除，那么验证者就有很大的概率相信证明者知晓<span
class="math inline">\(t(x)\)</span>这一知识。这就是多项式承诺系统的核心思想，在验证时验证者选定一个随机值<span
class="math inline">\(r\)</span>，证明者计算出<span
class="math inline">\(p(r), h(r)\)</span>发给验证者，如果满足<span
class="math inline">\(t(r)=\frac{p(r)}{h(r)}\)</span>则以较大的概率相信证明者。这是最简单直观的交互式多项式承诺系统。</p>
<h3 id="公开参数保护">公开参数保护</h3>
<p>如果想构造一个非交互式的零知识证明系统，那么就需要把一些参数预先公开发布，例如多项式的度数上限<span
class="math inline">\(d\)</span>（如果没有度数上限，那么可能会通过对大量数据的收集进行插值构造），随机数<span
class="math inline">\(r\)</span>，包括下文即将提到的位移参数<span
class="math inline">\(\alpha\)</span>。为了达成非交互式验证系统，这些参数一定会以明文可获取的形式公开，为了保护其中重要的信息，就要引入同态加密。</p>
<p>同态加密指的是对数据<span
class="math inline">\(M\)</span>进行加密，并支持有限的运算操作满足<span
class="math inline">\(f(enc(M))=enc(f(M))\)</span>。以较为简单的同态加密方式为例，一种支持加法与乘法的同态加密方法：<span
class="math inline">\(enc(x)=g^{x}\)</span>，加法转换为幂加法，乘法转换为幂乘法，即在有限域上的乘法和幂操作，即<span
class="math inline">\(enc(x + y) = g^{x+y},
enc(xy)=(g^{x})^{y}\)</span>。注意这里<span
class="math inline">\(g\)</span>需要在有限循环域上进行选择。</p>
<p>为了保护公开的校验随机数<span
class="math inline">\(r\)</span>，即采用同态加密的方式，以上述方法为例，在初始化时公开约定好的随机数根<span
class="math inline">\(g^{r^{1}}, g^{r^{2}} ...
g^{r^{d}}\)</span>，这样就可以防止攻击者根据<span
class="math inline">\(r\)</span>去泄漏额外的知识，或伪造多项式通过验证，证明者和验证者都通过这些随机数的若干次幂在有限循环域上计算，并不影响证明过程。（计算多项式只需要加法和乘法操作）</p>
<h3 id="多项式计算约束">多项式计算约束</h3>
<p>证明者理论上应该利用公开的<span
class="math inline">\(g\)</span>的幂去计算多项式的值，但是如果恶意证明者使用了其他方法想要通过概率碰撞的方式通过证明，或者以其他运算方式生成多项式的结果，会存在较低的概率被破解。为了解决这一问题，就需要证明Prover真的是用了这些值进行了加法和乘法运算。</p>
<p>以上述同态加密方法为例，使用KEA（Knowledge-of-Exponent
Assumption），引入位移参数<span
class="math inline">\(\alpha\)</span>，提供一套完全相同的基<span
class="math inline">\(g^{\alpha r^{1}}, g^{\alpha r^{2}} ... g^{\alpha
r^{d}}\)</span>，那么使用这组基计算出来的结果即为<span
class="math inline">\(g^{\alpha
t(r)}\)</span>。公开的参数为两组不同的基，要求验证者使用两组基做完全相同的运算操作，只有当验证者没有欺骗时才能满足<span
class="math inline">\(g^{\alpha t(r)} = g^{t(\alpha
r)}\)</span>，以保证证明者式通过多项式的基产生的证明。</p>
<h3 id="零知识保护">零知识保护</h3>
<p>进一步地进行零知识的保护，观察目前的方案：记证明者使用基计算出的值为<span
class="math inline">\(g^{h}, g^{p}\)</span>，使用位移基计算出的值为<span
class="math inline">\(g^{p&#39;}\)</span>，验证者需要验证以下等式：
<span class="math display">\[\begin{equation}
\begin{aligned}
g^{p} &amp;= (g^{h})^{t} \\
g^{p&#39;} &amp;= (g^{p})^{\alpha}
\end{aligned}
\end{equation}\]</span></p>
<p>因此证明者在生成证明时可以进一步地保护信息不泄漏，产生一个随机的混淆参数<span
class="math inline">\(\delta\)</span>，提供新的证明参数<span
class="math inline">\(((g^{p})^{\delta}, (g^{h})^{\delta},
(g^{p&#39;})^{\delta})\)</span>，仍然满足需要检验的约束条件，并且不会泄漏验证值的具体信息。</p>
<h3 id="双线性映射">双线性映射</h3>
<p>截止到目前为止仍然存在一个问题，为了支持任意验证者对证明进行验证，公开的参数CSR（Common
Reference String）中含有了验证需要的<span
class="math inline">\(g^{\alpha},g^{t(r)}\)</span>，那么验证者可以从这个值入手构造证明，只要通过上述等式就可以伪造。</p>
<p>为了解决这个问题，KZG引入了双线性映射，指的是一种映射<span
class="math inline">\(e\)</span>，可以将一个二元组映射到另一个域上，且满足<span
class="math inline">\(e(g^{a}, g^{b}) = e(g,
g)^{ab}\)</span>，可以将其简单地理解为同态哈希函数，在实践中往往采取椭圆曲线作为双线性映射，利用椭圆曲线的乘法与加法的性质进行映射。</p>
<p>有了双线性映射后，关键的CSR就在某种程度上进行了哈希映射，变为双线性映射后的参数值，验证者检验映射后的对应关系：</p>
<p><span class="math display">\[\begin{equation}
\begin{aligned}
e(g^{p}, g) &amp;= (g^{h}, g^{t}) \\
e(g^{p&#39;}, g) &amp;= e(g^{p}, g^{\alpha})
\end{aligned}
\end{equation}\]</span></p>
<p>这样证明者无法通过值简单地进行构造，就可以将参数直接公开，允许任意的验证者对证明进行验证。</p>
<h3 id="可信初始化">可信初始化</h3>
<p>现在一个完备的ZK-SNARK系统已经建立完毕，补充说明一下可信初始化的过程。显然，几个关键参数包括基、随机数、位移参数等是非常重要的安全参数，在生成后必须销毁，因此KZG系统需要一个可信的初始化过程，在生成后进行销毁。借助目前的多方可信计算技术，可以做到多人共同参与参数的生成，只要至少一个参与者销毁了自己的参数，就可以保证该参数不可被重新生成，这就是KZG系统的最小可信需求。</p>
<h2 id="知识系统的转换与构建">知识系统的转换与构建</h2>
<p>基于一个多项式，我们已经有了一个承诺系统，那么如何将一个实际问题转换为多项式呢？知识系统可以由多个不同的方法进行构建，下面介绍一种构建方式。</p>
<h3 id="rank-1-constraints-system">Rank-1 Constraints System</h3>
<p>首先要明确的是并不是任意问题都可以构造出知识系统，这里有相对严格的数学证明（想要继续了解的可以去进一步学习），这里以参考链接中给出的例子说明，Prover需要证明它知道<span
class="math inline">\(x^3 + x + 5 =
35\)</span>，因此可以构建出一个简单的逻辑关系表：</p>
<p><span class="math display">\[\begin{equation}
\begin{aligned}
t_1 = x * x \\
t_2 = t_1 * x \\
t_3 = x + 5 \\
out = t_2 + t_3
\end{aligned}
\end{equation}\]</span></p>
<p>可以注意到这里构建的逻辑关系表遵循了类似于LLVM的SSA原则（这是为了转换为R1CS），左侧只有一个元素，右侧是两个元素执行某种运算，得到了这样的Circuit之后就可以据此转换为一个多项式形式的知识证明系统。</p>
<p>R1CS指的是秩为1的约束系统，其由向量组<span class="math inline">\((a,
b, c)\)</span>组成，该系统的解向量<span
class="math inline">\(s\)</span>满足<span class="math inline">\(sa * sb
- sc = 0\)</span>（此处为内积，即<span class="math inline">\(sa = \sum
s_{i} *
a_{i}\)</span>），那么基于上述生成的逻辑电路表，我们就可以构建出一个R1CS。其中向量<span
class="math inline">\(s\)</span>的每一项都对应着逻辑电路表中的一个变量，将等式转换为<span
class="math inline">\(a, b, c\)</span>。不妨设逻辑电路表中有<span
class="math inline">\(n\)</span>个变量，<span
class="math inline">\(m\)</span>个参数，那么就可以向量<span
class="math inline">\(a, b, c\)</span>将含有<span
class="math inline">\(n\)</span>行<span
class="math inline">\(m\)</span>列，每一列都代表着一个不同的约束条件。</p>
<h3 id="qap-linear-pcp">QAP &amp; Linear PCP</h3>
<p>为了保证零知识性，下一步需要对向量<span class="math inline">\(a, b,
c\)</span>进行模糊处理，首先对向量进行随机扩展（不改变已经产生的值），之后通过拉格朗日插值法将向量转换为多项式，保证在<span
class="math inline">\(x=i\)</span>的取值上可以得到<span
class="math inline">\(a_i, b_i,
c_i\)</span>。这样原始向量就被保护起来，并且验证者可以通过选取<span
class="math inline">\(x\)</span>，使用解向量在多项式的某一点进行验证，是否真的满足<span
class="math inline">\(sa_i * sb_i - sc_i =
0\)</span>，这一验证过程由于可以合并多项式，具有非常低的时间复杂度，符合ZK-SNARK的代价需求。就由此，我们就完成了知识系统的转换与构建。</p>
<h2 id="补充">补充</h2>
<p>在了解零知识证明体系的过程中，我也了解到很多本文未涉及的密码学算法，包括将交互式证明转为非交互式的Fiat-Shamir协议（涉及随机预言机）等。我对这部分知识的了解并不深入，感兴趣的同学可以继续学习。</p>
<p>参考链接：</p>
<ol type="1">
<li><a
href="https://medium.com/@VitalikButerin/quadratic-arithmetic-programs-from-zero-to-hero-f6d558cea649">Quadratic
Arithmetic Programs: from Zero to Hero</a></li>
<li><a href="https://arxiv.org/abs/1906.07221">Why and How zk-SNARK
Works</a></li>
<li><a href="https://rdi.berkeley.edu/course/zkp/s23">CS294/194-238 Zero
Knowledge Proof</a></li>
</ol>
]]></content>
      <categories>
        <category>Paper Note</category>
      </categories>
      <tags>
        <tag>Ethereum</tag>
        <tag>Crypto</tag>
        <tag>Zero Knowledge Proof</tag>
        <tag>Study</tag>
      </tags>
  </entry>
  <entry>
    <title>ghOSt Fast &amp; Flexible User-Space Delegation of Linux Scheduling</title>
    <url>/posts/d7cefcc1.html</url>
    <content><![CDATA[<p>AOS课程选题。</p>
<h2 id="背景">背景</h2>
<p>定制的调度策略可以提高许多关键指标，较重要的有延迟、吞吐量、实时性，能耗，缓存干扰，安全性等。但是一方面定制的调度策略本身不具备普适性，需要对具体情况进行分析，另一方面硬件的发展如多核，异构计算等，也对调度程序和调度模型提出了挑战。</p>
<p>以Linux操作系统举例分析，目前的调度算法和策略存在以下问题：</p>
<ol type="1">
<li><p>操作系统默认采取CFS（Complete Fair
Schedule）算法进行线程的调度，内核中的调度模块允许用户通过设定线程的nice值来对调度策略提出建议，但是这存在着不足。首先，用户只能在一定程度上提供建议，调度模块并不保证完全按照用户的建议进行调度。另外，用户无法突破CFS调度算法的框架，无法采用一些复杂的动态调度算法，如监测关键请求发生。因此Linux操作系统目前还不能支持用户执行自定义的调度策略。</p></li>
<li><p>如果由用户重写内核调度模块的话，一方面用户需要掌握大量的汇编、C语言、内核知识，需要进行大量的开发工作，另一方面内核模块的bug会直接导致操作系统的崩溃，考虑在用户自定义策略需求旺盛的云端，一段时间的机器崩溃和修复往往是厂商或用户不可承受的巨大成本。</p></li>
<li><p>目前用户可以通过eBPF（Extended Berkeley Packet
Filter）的方式在内核态中执行经过审查的安全代码，即可以用这种方式实现用户自定义的调度策略。但是eBPF的速度较慢，可执行指令数具有上限，可访问的内核数据有限，不符合调度线程的需求。</p></li>
</ol>
<p>因此出现了对于可自定义调度策略调度框架的需求。ghOSt框架基于上述实现用户自定义调度策略的问题，在设计时便列出了以下目标：</p>
<ul>
<li><p>调度策略应易于实现和测试，减轻用户的开发使用难度</p></li>
<li><p>调度框架应注重于性能和效率，实现微妙级别的调度并保持调度策略的效率</p></li>
<li><p>调度框架应支持现代的调度模型，支持细粒度的调度单位，适应现代的计算集群</p></li>
<li><p>调度框架应该支持多种调度策略并行互不干扰</p></li>
<li><p>调度框架应该提供策略部署、升级、回滚甚至崩溃的支持</p></li>
</ul>
<h2 id="贡献">贡献</h2>
<p>在本文中我们提出了ghOSt，它是一个原生的系统调度线程框架，将执行策略委托给了用户态程序执行。ghOSt的目标是从根本上改变调度策略被设计，应用和部署的流程，与此同时ghOSt提供了便捷的使用和简易的部署支持，能实现微妙级别的调度，支持多种调度策略并行互不干扰。通过ghOSt，用户可以轻松地实现自定义的调度策略，并快捷安全地部署在任何场景，并且几乎不添加额外的开销。在本文中我们还提出了中心化的调度模型，相比于传统的CPU调度模型，中心化模型更加符合现实应用场景，ghOSt框架对二者都做出了支持。</p>
<div style="width:70%;margin:auto">
<img src="/posts/d7cefcc1/centralized.png" class="" title="Centralized Model">
</div>
<h2 id="原理">原理</h2>
<p>ghOSt由内核态和用户态两部分组成，内核态利用权限向用户态提供内核中的线程信息，而用户态则接受内核态的信息，执行用户编程的调度策略，将调度结果发还给内核态予以执行。由此ghOSt便支持了用户自定义调度策略的部署和执行，基本满足了框架的设计目标。</p>
<div style="width:70%;margin:auto">
<img src="/posts/d7cefcc1/architecture.png" class="" title="ghOSt architecture">
</div>
<h3 id="内核态向用户态通信">内核态向用户态通信</h3>
<p>内核态负责向用户态提供必要的调度信息，将线程信息如task_struct发给用户态辅助调度策略执行。因此用户态可以使用任何语言实现，只需要接受给定的信息，生成相应格式的调度结果即可，这便减轻了用户的开发负担，不必深入汇编，内核等相对底层的实现。</p>
<p>内核态通过消息队列的方式向发送信息，其中包括了像线程创建，线程唤醒等信息，以支持用户态编写较为复杂的实时动态调度算法，以服务器场景为例，用户自定义调度策略可以监测关键请求的来临，通过针对分配资源的方式降低服务的尾延迟。</p>
<p>在用户态收到了内核信息后，执行用户编写的调度策略，得到调度结果后发送中断以切换到待执行的线程。ghOSt框架也为用户态提供了相应的syscall
API来支持消息队列的操作，在多CPU模型中，可以通过这些接口实现CPU间线程的迁移和管理。</p>
<h3 id="用户态向内核态通信">用户态向内核态通信</h3>
<p>用户态对应着一个或多个的处理器，用户态中包含用户所编写的调度策略，在收到内核态发送的线程信息后执行用户调度策略，将生成的结果回发至内核态，并通过syscall引发中断，调度到待执行的线程。</p>
<p>在调度开始后，调度器作为一个线程就让渡了自身的执行权，这时用户态是处于等待执行的状态，如果此时内核态更新了线程的状态信息，例如一个被策略标记为关键的线程开始执行了，此时就应该改变调度策略。为了支持复杂的动态的调度策略，ghOSt框架借鉴了分布式系统的设计，在消息中加入了时间戳。内核态向用户态发送的信息具有自增的序列号，而用户态回复的调度结果也需要带有此信息号，只有内核态收到的事务序列号等同于发出的消息序列号时才会视为一次交互，否则会返回错误令用户态重新生成调度策略。这样就为调度策略的设计提供了较为灵活的动态性，可以根据情况灵活地调整。</p>
<h2 id="实现">实现</h2>
<h3 id="错误容忍技术">错误容忍技术</h3>
<p>飞地（Enclave）是指一个隔离的线程运行环境，是由硬件支撑的隔离技术。ghOSt框架使用了飞地技术，每个用户端与他们所负责调度的程序运行在同一个飞地当中。由此，ghOSt框架就可以大幅降低用户开发，部署，测试的难度，即使用户编写了错误的调度算法也不会影响到其他飞地以及内核的正常运行，即使发生了崩溃也只是导致这一飞地中的线程组出现调度问题。另外，多个互不干扰的飞地可以让ghOSt框架支持多种调度策略并行，不同的线程组使用不同的调度策略。</p>
<p>从设计的角度说，ghOSt也为调度崩溃留有冗余机制，ghOSt的调度类实现为了系统调度的低优先级调度类，当ghOSt行为发生异常后将会自动转交给默认的调度器进行调度，实现了较强的错误容忍机制。ghOSt也实现了更新机制，当用户端的策略需要进行调整时，会拉起一个新的用户端，直至新的用户端完成交互后，旧的客户端将结束。</p>
<h3 id="中心化模型">中心化模型</h3>
<p>为了满足中心化模型下多处理器调度场景，ghOSt框架需要管理本地或远程的多计算集群，由一个全局的用户态调度多个计算集群或CPU执行任务，控制各计算群调度策略以及计算群间的任务调度。在这种场景下就要仔细地考虑每个计算群的执行状态，因此ghOSt借鉴了数据库的概念，采用了transaction(事务)的方式来确认用户态发给内核态的信息，用户态产生的调度结果通过事务发送给内核态，在事务提交后才视为一次有效的调度，如果远程计算集群无法立即回应调度，那么事务不会被确认提交，内核态调度程序发现这一情况可以进行处理，重新发送调度要求或者转交由其他处理器执行。考虑到调度框架的性能要求，事务模型采用共享内存的方式实现以加快速度。</p>
<p>在中心化模型中，ghOSt框架会维护一个全局的用户态程序，全局的用户态程序会接受多个调度请求，然后将涉及的中断打包发给处理器，以减少信息传递的代价，这里利用了处理器硬件支持的batch
interrupt功能。在涉及到计算集群间的任务迁移时，全局的用户态程序会发送IPI（Inter
Processer Interrupt），触发迁移目的地集群中断实现。</p>
<h2 id="实验">实验</h2>
<p>ghOSt是一个用户自定义调度策略执行框架，因此本文的实验评估基于以下三个方面开展：</p>
<ol type="1">
<li>ghOSt框架带来的额外开销有多大？</li>
<li>ghOSt框架执行自定义策略调度效果如何？</li>
<li>ghOSt框架是否具有实用性？</li>
</ol>
<h3 id="性能与效率">性能与效率</h3>
<p>经过测试，ghOSt框架的调度开销是微秒级别，相比于内核原生的调度器并没有添加过于沉重的开销。在中心化模型下，较大的开销是在remote
schedule上，对多个处理器进行调度并通过处理器间中断（IPI）的方式进行调度。</p>
<p>同时作者也比较了使用ghOSt执行调度策略和定制的调度器之间的差距，实验表明ghOSt几乎可以达到定制调度器的调度效果，但是当然在性能上有所不足，这也是灵活性与性能的取舍。</p>
<div style="width:70%;margin:auto">
<img src="/posts/d7cefcc1/time.png" class="" title="time consumption">
</div>
<h3 id="实际场景应用">实际场景应用</h3>
<p>作者选取了Google Snap 和 Google
Search任务来模拟真实的云端使用场景，以说明ghOSt框架的可用性。这里以Google
Search测试为例，使用ghOSt执行用户自定义的调度策略，可以看到整体的吞吐量相矫CFS没有明显变化，但是自定义策略在AB两个案例中可以显著的降低尾延迟。这说明了ghOSt框架在真实场景下是可用的，可以大幅度提升处理效率。这里的实验C作者分析是由于定制策略还有待改进，使得提升效果不明显。</p>
<div style="width:70%;margin:auto">
<img src="/posts/d7cefcc1/latency.png" class="" title="latency">
</div>
<h2 id="总结">总结</h2>
<p>ghOSt框架实现对了用户自定义策略的支持，将调度任务又内核态委托给了用户态执行，简化了用户开发、测试、部署的流程，在几乎不增加调度性能开销的基础上实现了功能强大的调度框架支持，为多调度策略，中心化模型提供了良好的支持。</p>
<p>通过ghOSt框架的实验测试，在云端等场景下存在着巨量的通过改进调度策略而提升执行效率的空间，这也为调度方向后续的研究提供了参考。</p>
]]></content>
      <categories>
        <category>Paper Note</category>
      </categories>
      <tags>
        <tag>OS</tag>
        <tag>Scheduler</tag>
      </tags>
  </entry>
  <entry>
    <title>《Computer Achitecture A Quantitative Approach》读书笔记</title>
    <url>/posts/fedbab77.html</url>
    <content><![CDATA[<h1
id="chapter-1---fundamentals-of-quantitative-design-and-analysis">Chapter
1 - Fundamentals of Quantitative Design and Analysis</h1>
<h2 id="一些物理工艺相关的知识">一些物理/工艺相关的知识</h2>
<ul>
<li>Trends in Technology
<ul>
<li>CPU
的片上缓存是&lt;10ns级别的，而主存DRAM是数十ns级别的，网络接口是微秒级别的，而磁盘则是毫秒级别的，因此一次
cache miss 就对应几十个甚至上百个周期的停滞。</li>
</ul></li>
<li>Trends in Power and Enenergy
<ul>
<li>TDP 是 Thermal Design Power
的缩写，不代表峰值功耗（可能为1.5倍TDP），也不是平均功耗（通常低于TDP），而是给予散热系统的指导数据。</li>
<li>CMOS 工艺芯片的主要功耗来自于晶体管状态的翻转，<span
class="math inline">\(P_{dynamic} \propto fCV^{2}\)</span>，<span
class="math inline">\(f\)</span>是时钟频率，<span
class="math inline">\(C\)</span>是电容，<span
class="math inline">\(V\)</span>是电压，而 CPU
的时钟频率又往往与电压成正比，动态的功耗可能与电压呈三次方的线性关系。另一方面，随着晶体管数量的增加，静态的功耗也成为了较大的开销部分，即使晶体管没有翻转，仍然有电流通过，<span
class="math inline">\(P_{static}=IV\)</span>。因此电源管理系统甚至直接关闭掉某一个区域的电源以节约功耗。</li>
</ul></li>
<li>Trends in Cost
<ul>
<li>圆晶（wafer）是由硅单质生长出来的，受限于工艺为圆形，按照面积大小切割为
die
，圆晶上会有随机分布的缺陷存在，再结合数字电路的复杂度形成工艺良率。工业中往往会对
memory 进行冗余设计，即通过冗余的 cell
单元来避免良率带来的影响。芯片在切割后还要经过测试、封装、封装后测试等步骤，这都给芯片制造添加了额外的成本。</li>
</ul></li>
</ul>
<h1 id="appendix-b---review-of-memory-hierarchy">Appendix B - Review of
Memory Hierarchy</h1>
<h2 id="introduction">Introduction</h2>
<p>在评判存储体系设计时，考虑基础的单发射处理器模型，影响的指标有<strong>命中率</strong>、<strong>未命中惩罚</strong>和<strong>访存指令占比</strong>，三者综合可以计算出存储体系对于处理器性能的影响，进而评价其设计的好坏。但是随着现代处理器体系结构的发展，越来越多的因素需要被一并考虑进来：</p>
<ul>
<li><p>命中率基于数据统计，表示存储体系在面对随机访问时的命中概率，比较直观的定义是访存指令命中的概率，即命中次数除以执行的访存指令数量。但是在现代处理器中，投机执行策略可能会发射不会被提交的错误指令，错误的访存指令是否命中符合命中率的定义，然而却不直接影响性能。<strong>因此严格来说，从性能的角度考虑，应该使用命中次数除以实际提交的指令数量作为命中率，更能反应出存储体系设计对于性能的影响。</strong>同时，这也启发了进一步的设计思考：错误发射的访存指令是否应该影响存储体系的状态？</p></li>
<li><p>未命中惩罚通常是基于多级Cache的延迟计算得出，进一步引出的思考是为什么往往采用串行方式逐级查询？在L1命中率较高的情况下，广播查询并设计逐级匹配的逻辑电路在功耗、性能上是不划算的。再继续考虑延迟的计算，在非阻塞缓存设计下，失配的查询会被暂存以继续提供服务，那么延迟代价就存在合并的可能性，在某个时间多条失配地址被一起发送到下级缓存查询。<strong>从准确的角度考虑，以硬件设计得到的失配延迟来计算是不准确的，需要根据实际数据对平均未命中惩罚进行计算。</strong>缓存、甚至是核内LSU的策略设计和微架构设计都会造成影响。</p></li>
<li><p>访存指令占比则与指令集设计、编译器优化强相关，反而是与硬件设计技术无关。</p></li>
</ul>
<p>在了解一个未知的存储体系设计时，可以通过四个问题入手：block
placement, block identification, block replacement, write
strategy，这四个问题涵盖了大部分可能出现的情况，以基础的cache结构为例：</p>
<ul>
<li><p>block placement：描述数据如何确定放置的位置，也就是定位cache
line。最简单的cache结构是direct mapped，根据数据地址的low
bit写入相应的cache line。进一步的有set
associative（组相连），每一个set里面可以含有k个cache
line，就是k-路组相连，表示每一个block按照low
bit放入对应的set中，可能有k个位置。全相连则表示任何block都可以放在任何位置。<strong>在真实世界中，较多使用的是2-way和4-way组相连。</strong></p></li>
<li><p>block identification：描述如何确认cache
line存储的就是要寻找的数据。low bit作block offset，用于在cache
line内截取需要的数据，其余继续划分low
bit作(set)index，剩下的为tag，比较的时候按照index定位set，再逐一比较tag。<strong>这里还要确认一下用于寻址的地址是physical还是virtual，转换的过程在哪里进行。</strong>在传统面试的计算题中，cache
size描述的是配套的RAM的大小，所有其他信息都不包含在范围之内，用它除以cache
line大小就可以算出cache line数目，再进一步根据组相连情况划分。</p></li>
<li><p>block
replacement：描述如何寻找换出的block。常见的思路有伪随机，LRU，FIFO，通常来说LRU表现效果最好，在硬件上通常使用Pseudo-LRU实现方式：</p>
<ul>
<li><p>树伪LRU：维护二叉树，每一个非叶子结点具有1bit，表示最近一次使用的cahce
line位于左侧还是右侧，在每一个access过程更新路径上的bit，需要替换时按照指示即可寻找LRU近似，时间复杂度O(logN)。</p></li>
<li><p>单位伪LRU：每个cache
line一个bit，表示最近是否被访问过，当一次access后cache
line中所有的bit都为1了，则重置这次访问以外的所有bit，需要替换时在bit为0的cache
line中使用伪随机寻找，时间复杂度O(1)。</p></li>
</ul></li>
<li><p>write strategy：描述写入数据如何处理cache和low-level
memory的关系。由于写的占比小于读，因此对于读的优化很重要，在读的过程中可以同时取数据和比较tag，在写的过程中CPU不需要等待操作完成。按照同步性可以分为两种：</p>
<ul>
<li><p>write-through &amp; no
write-allocate：写入数据时同时修改cache和low-level
memory的状态，当发生write
miss时只修改low-level的数据。这样做的好处是不需要考虑多级存储之间的数据一致性问题，便于其他核心/设备读取。</p></li>
<li><p>write-back &amp; write-allocate：写数据只修改在cache中，通过dirty
bit记录状态，当数据换出cache时同步修改到low-level memory，当发生write
miss时只影响cache内。这样做的好处是可以利用空间局部性减少miss，可以通过write
buffer减少存储间通信的代价，并且统一了read miss &amp; write
miss的行为模型，但是需要考虑多核情况下一致性的维护。一般来说还有一个优化，<strong>当发生read
miss时，low-level memory的数据可以优先传递需要的8
byte数据，尽量减少CPU等待时间，再传输cache
line中的其他数据。</strong></p></li>
</ul></li>
</ul>
<p>通常来说，cache会被划分为Icache和Dcache，其中Icache的miss率可以做到很低，这样划分后相比于unified
cache在小规模上可以去的更低的失配率。</p>
<h2 id="cache-performance">Cache Performance</h2>
]]></content>
      <categories>
        <category>Book Note</category>
      </categories>
      <tags>
        <tag>Architecture</tag>
      </tags>
  </entry>
  <entry>
    <title>《Mastering Ethereum》读书笔记</title>
    <url>/posts/10fe4eb6.html</url>
    <content><![CDATA[<p>根据领域的需求买了一本《精通以太坊》，看的过程中顺便整理一下之前不知道的知识。</p>
<h2 id="以太坊背后的密码学">以太坊背后的密码学</h2>
<p>私钥是用户于本地随机生成的256位随机数字，私钥对应着以太坊账户的控制权和所有权，因此需要严格保密。私钥的生成更依赖于密码学安全的伪随机数生成器（如CSPRNG），在以太坊中私钥可以是<span
class="math inline">\([1, n-1]\)</span>中的任何数字，其中<span
class="math inline">\(n\)</span>代表了以太坊使用的椭圆曲线的阶，为<span
class="math inline">\(1.158 * 10^{77}\)</span>。</p>
<p>以太坊使用椭圆曲线保证非对称密码的安全性，采用<code>secp256k1</code>的标准，对应椭圆曲线为<span
class="math inline">\(y^{2} \equiv (x^{3} + 7) \ mod \
p\)</span>，其中<span class="math inline">\(p=2^{256} - 2^{32} - 2^{9} -
2^{8} - 2^{7} - 2^{6} - 2^{4} -
1\)</span>是一个大素数。椭圆曲线上的两点<span class="math inline">\(p1,
p2\)</span>，定义<span
class="math inline">\(p1+p2=p3\)</span>，其中<span
class="math inline">\(p3\)</span>为点<span class="math inline">\(p1,
p2\)</span>连线与椭圆曲线的唯一交点，特别地若<span
class="math inline">\(p1=p2\)</span>，那么<span
class="math inline">\(p3\)</span>则为过点<span
class="math inline">\(p1\)</span>的切线与椭圆曲线的唯一交点。根据加法在椭圆曲线上的定义，定义正整数乘法<span
class="math inline">\(k *
p=p+p+....+p\)</span>，得到的仍然是椭圆曲线上的唯一点。</p>
<p>公钥则是由私钥计算产生的椭圆曲线上的一个点，记私钥为<span
class="math inline">\(k\)</span>，公钥为<span
class="math inline">\(K\)</span>，那么则有<span class="math inline">\(K
= k * G\)</span>，其中<span
class="math inline">\(G\)</span>是一个椭圆曲线上的常量点。由于椭圆曲线的定义，椭圆曲线上的乘法是难以进行逆运算的，因此保证了私钥的安全性。根据SECG发布的一种序列化编码方式，用四种可能的前缀来表示椭圆曲线上的点位，如下表所示，以太坊使用未压缩点编码公钥，即<code>0x04 + x + y</code>的方式编码。</p>
<table>
<thead>
<tr>
<th style="text-align: center;">前缀</th>
<th style="text-align: center;">含义</th>
<th style="text-align: center;">长度（字节）</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align: center;">0x00</td>
<td style="text-align: center;">无穷远点(0,0)</td>
<td style="text-align: center;">1</td>
</tr>
<tr>
<td style="text-align: center;">0x04</td>
<td style="text-align: center;">未压缩点，保存<span
class="math inline">\(x, y\)</span>坐标</td>
<td style="text-align: center;">65</td>
</tr>
<tr>
<td style="text-align: center;">0x01</td>
<td style="text-align: center;">偶数<span
class="math inline">\(y\)</span>压缩点</td>
<td style="text-align: center;">33</td>
</tr>
<tr>
<td style="text-align: center;">0x03</td>
<td style="text-align: center;">奇数<span
class="math inline">\(y\)</span>压缩点</td>
<td style="text-align: center;">33</td>
</tr>
</tbody>
</table>
<p>以太坊的地址则使用哈希算法对公钥进行加密（特别注意此处的公钥应带有<code>04</code>前缀），以太坊中使用的哈希加密算法为<code>keccak-256</code>算法，具体的细节在另一篇博文中讨论。通过<span
class="math inline">\(keccak(K)\)</span>我们可以得到256位结果，选出大端序中的末20字节(160位)作为账户的地址</p>
<p>为了避免转账时可能发生的地址输入错误，社区提出了<code>EIP-55</code>提案，对十六进制编码的地址进行大写检验。对于十六进制小写编码的地址<span
class="math inline">\(addr\)</span>进行哈希运算，得到大端序下的高20字节逐位检验，如果十六进制编码的哈希结果大于等于8，那么则把对应的十六进制编码的地址修改为大写（如果为字母的话）。采用大小写混合的地址后，一旦发生了地址输入错误情况，则可以通过哈希值反向对地址的合法性进行核验，由于哈希函数对于输入的变化极为敏感，即使发生了一位的错误也会使哈希结果发生巨大的变化，<code>EIP-55</code>可以有效地防止可能发生的地址输入错误。</p>
<h2 id="钱包">钱包</h2>
<p>如果用户只采用同一个地址进行转账交易的话，很容易通过地址进行交易的追踪，为了实现在去中心化网络上的匿名性，用户可以通过钱包技术管理多个派生出的账户，从而避免通过地址进行的追踪。目前的钱包大多采用了确定性钱包，即通过一个种子密钥派生出多个可用的子钥，并利用多个可用的子地址进行交易。</p>
<p>简要介绍一下助记词标准（BIP-39）。在用户创建钱包时，钱包会生成一个密码学安全意义上的随机数，记为<span
class="math inline">\(S\)</span>（长度一般为128位或者256位），取出<span
class="math inline">\(SHA-256(S)\)</span>的前<span
class="math inline">\(S/32\)</span>比特作为校验值附加在<span
class="math inline">\(S\)</span>的末尾，按照11比特一组分为<span
class="math inline">\(3S/32\)</span>组，在预先选取出的2048个简单英文单词中进行映射，形成助记词组。若用户忘记了钱包的密码，可以通过助记词可以恢复出钱包生成的随机数。分层确定性钱包（HD）由一个512比特的种子密钥开始生成，使用助记词组作为第一个参数，可选的盐（默认为"mnemonic"，可以在后附加用户指定的密码）作为第二个参数，使用密钥扩展算法PBKDF2进行2048轮HMAC-SHA512哈希运算得到。<strong>需要特别注意的是</strong>此处的密码与设立钱包时要求用户填写的密码不同，若设立时的密码丢失，可以通过助记词进行恢复，这也说明了这个密码仅作钱包的登录使用，并不参与到后续种子密钥的生成过程。</p>
<p>有了钱包的种子密钥后就可以根据曾几时确定性钱包标准（BIP-32）进行密钥的派生了，其中种子的左256位为根私钥，右256位为根链码，扩展派生出的密钥形成树形结构，根据扩展时使用的索引进行编码，不同的层级间用<code>/</code>隔开，如下图所示。</p>
<div style="width:70%;margin:auto">
<img src="/posts/10fe4eb6/BIP-32.png" class="" title="BIP-32 Standard">
</div>
<p>比较直观的派生过程如下图所示，将父公钥，父链码和索引作为哈希函数的输入，得到的512位哈希输出分为左右两部分，分别记为<code>l, r</code>，<code>l</code>与父私钥<code>p</code>相加得到子私钥<code>c=l+p</code>，<code>r</code>作为子链码用于下一级密钥的派生。为了方便钱包间的导入导出，将密钥与对应的链码结合起来称为扩展密钥，使用Base58Check进行编码，其中扩展私钥以<code>xpriv</code>开头，扩展公钥以<code>xpub</code>开头。</p>
<div style="width:70%;margin:auto">
<img src="/posts/10fe4eb6/derive-Private.png" class="" title="Parent Private Key">
</div>
<p>为了在不可信赖环境下进行部署，利用椭圆曲线运算上的结合律，可以不经过父私钥，只通过父扩展公钥生成子公钥。因为生成方法为<code>c=l+p</code>，根据公钥生成方式有<code>C=G*c, P=G*p</code>，因此有<code>C=G*(l+p)=G(l)+P</code>，将生成点与哈希结果的左半部分相乘，再加上父公钥即可得到派生出的子公钥，这样就避免了私钥的部署。</p>
<div style="width:70%;margin:auto">
<img src="/posts/10fe4eb6/derive-Public.png" class="" title="Parent Public Key">
</div>
<p>考虑如果某一个子私钥发生了泄露，在父扩展公钥暴露的情况下，可以对索引进行暴力破解，从而根据<code>c=l+p</code>的逆运算得到父私钥，使得由该父节点派生的所有密钥发生泄漏。为此引入了增强派生算法，由父私钥，父链码和索引生成子一级的私钥和链码，由于私钥不会被部署也就隔断了子一级向上推断的可能，当然缺点是私钥无法像公钥一样部署在服务器上，因此通常在最根一级使用增强派生算法，或在需要隔绝父子间推导关系的场景使用，以增强钱包的安全性。</p>
<p>为了区别两种派生方法，规定索引最高位为0代表普通派生方法，为1代表增强派生算法，特别地在表示时，<code>m/x/y</code>为密钥<code>m</code>派生的第<code>x</code>个子密钥的第<code>y</code>个子密钥，<code>m/x'/y</code>表示密钥<code>m</code>派生的第<code>x+0x80000000</code>个增强密钥的第<code>y</code>个子密钥。</p>
<p>根据BIP-44标准，定义树型层级的含义为<code>m / purpose' / coin_type' / account' / change / address_index</code>。其中<code>purpose'</code>总是设为<code>44'</code>，<code>coin-type'</code>用以区分不同的币种，以太坊是<code>60'</code>，<code>account'</code>用以区分不同的逻辑账户，<code>change</code>用以区分区块链的找零地址，<code>address_index</code>则是同一个逻辑账户下的不同地址。</p>
<p>参考链接：</p>
<ol type="1">
<li><a
href="https://medium.com/@robbiehanson15/the-math-behind-bip-32-child-key-derivation-7d85f61a6681">The
math behind BIP-32 child key derivation</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/33070394">比特币区块链（六） |
钱包的技术细节之从种子创建HD钱包</a></li>
</ol>
<h2 id="交易">交易</h2>
<p>交易是唯一能够触发以太坊区块链状态发生改变的行为（矿工挖矿除外），交易包含以下内容：</p>
<ul>
<li><code>nonce</code>：序列编号，表示该次交易是发起者账户的第几次交易，用于抵御重放攻击</li>
<li><code>gas price</code>：交易发起方愿意支付的gas价格，通常来说越高的价格对应着越快被矿工打包进区块</li>
<li><code>gas limit</code>：交易发起方愿意支付的gas上限</li>
<li><code>recipient</code>：目标以太坊地址</li>
<li><code>value</code>：交易发送给的以太币数量</li>
<li><code>data</code>：交易中附加的可变长数据</li>
<li><code>v, r, s</code>：交易附加的数字签名部分，用于验证交易安全性</li>
</ul>
<p>交易在以太坊中采用RLP（递归长度前缀）标准编码，打包为大端的，8比特倍数长度的二进制数据。</p>
<p><code>nonce</code>满足了用户按序执行交易的需求，假设用户发出多笔交易，希望可以按照顺序依次执行，但按序发出的交易数据在区块链网络中被确认的顺序只取决于它们沿着P2P网络到达确认节点的顺序，因此通过<code>nonce</code>可以保证用户的交易按序执行。如果节点收到了不符合历史<code>nonce</code>的交易，则会放入待确认交易列表，直至空缺的<code>nonce</code>都有对应的交易被确认后再做执行。同时<code>nonce</code>也作为计数器抵御了重放攻击，避免同一笔交易被恶意多次广播。但是<code>nonce</code>的这一设计也为并行化构造交易带来了难题，目前只能采用串行的方法进行多笔交易的生成。</p>
<p>交易的目标地址可以是外部账户或者合约账户，可以通过<code>data</code>段对合约账户的函数进行调用，调用时按32字节对齐，以目标函数原型定义（例如<code>withdraw(uint256)</code>）的Keccak-256哈希值的前4个字节开头，表示调用的函数，以参数作为结尾。特殊地，向<code>0x0</code>地址发送交易会将<code>data</code>段中的数据创建为一个合约，向<code>0x00...00dEaD</code>发送以太币是销毁以太币的含义，当然发送到一个任意地址也可能是在销毁以太币。</p>
<p>为了确保交易的安全性，以太坊采用了椭圆曲线数字签名算法（ECDSA）给交易附加上了数字签名，数字签名提供了身份验证，不可否认和数据完整性的支持。基于数据签名算法的性能考虑，数字签名一般只作用于待发送消息的哈希结果，即<code>Sig=F_&#123;sig&#125;(Keccak(message), k)</code>，其中<code>k</code>是用于签名的私钥，<code>message</code>则是经过打包的数据。</p>
<p>为了保证用户使用私钥对多笔交易进行签名不会导致安全性的降低，在每次签名时，除了用户本身的私钥<code>k</code>，还会采用一个随机生成的私钥<code>q</code>共同加密，得到<span
class="math inline">\(s \equiv q^{-1}(Keccak(m) + r*k) \ mod
\  p\)</span>，其中<code>p</code>是椭圆曲线上的素数阶，<code>r</code>是私钥<code>q</code>对应的公钥<code>Q</code>的横坐标，生成的签名<code>Sig=(r, s)</code>。</p>
<p>在验证数字签名时可以通过<code>r</code>对信息进行核验，如果签名有效则有<span
class="math inline">\(q \equiv s(Keccak(m) +
rk)\)</span>，私钥不可能公开，于是同乘生成点<code>G</code>得到<span
class="math inline">\(Q = Keccak(m)*sG +
rK\)</span>，即可通过<code>Q</code>对<code>r</code>进行验证数字签名的有效性，保证是从公钥<code>K</code>对应账户中发出的交易。</p>
<p>但是注意到交易中并没有附加<code>from</code>项，我们需要从签名中先恢复出交易发起方的公钥并确定其账户，这也是数字签名设计的比较巧妙的地方：<strong>引入了随机生成的密钥对，并与账户持有的密钥对建立了对应的联系</strong>。加密时的<span
class="math inline">\(q^{-1}\)</span>等价于两侧都乘上一个私钥，这样在不泄露私钥的前提下，同乘上生成点<code>G</code>就可以转换为公钥间的联系，即<span
class="math inline">\(sQ = rK +
Keccak(m)*G\)</span>，即可算出交易发起的账户公钥为<span
class="math inline">\(K=r^{-1}(sQ - Keccak(m) * G)\)</span>。</p>
<p>不过基于椭圆曲线关于x轴对称的性质，从横坐标<code>r</code>恢复公钥<code>Q</code>时会对应两个点，可以通过<code>s</code>反过来选择公钥<code>K</code>。为了提高效率，在数字签名中使用<code>(v, r, s)</code>，约定<code>v</code>使用<code>0, 1</code>表示究竟采用曲线上的哪个点，在较老的链上使用的是27或28（27是比特币采用的随机数），经过<code>SpuriousDragon</code>分叉后采用35或是36。</p>
<p>EIP-155提案又为交易增加了<code>chainID, r(=0), s(=0)</code>的结构成员，其中<code>chainID</code>是为了抵御不同以太坊网络间的重放攻击。</p>
<p>目前以太坊没有原生的多签名交易支持，需要智能合约进行实现。交易的隐秘性通过P2P的传播来保护，任何一个消息的传递都可能来自发起者或是网络中的传播节点，攻击者需要掌握大多数节点才能确认交易发起者的网络地址。</p>
]]></content>
      <categories>
        <category>Book Note</category>
      </categories>
      <tags>
        <tag>Ethereum</tag>
        <tag>Crypto</tag>
        <tag>Security</tag>
      </tags>
  </entry>
  <entry>
    <title>《Vivado数字设计》读书笔记</title>
    <url>/posts/e29e40ff.html</url>
    <content><![CDATA[<h2 id="数字逻辑基础">数字逻辑基础</h2>
<h3 id="数字电路与晶体管">数字电路与晶体管</h3>
<p>数字电路中的信号变化是不连续的，信号的电压取值只有VCC和GND两种情况，表示逻辑高/低电平，数字电路状态"0,
1"定义了两个宽范围电压区域，在区域内电压的变化不会改变数字信号的状态。因此相比于模拟电路信号，数字信号具有抗干扰能力强和处理方式灵活的显著优势。</p>
<p>为了对电路进行控制，需要引入数字电路的<strong>开关</strong>，在现代数字电路中的晶体管开关称为金属氧化物半导体场效应晶体管（Metal
Oxide Semiconductor Field Effect Transistor,
MOSFET），它是三端口器件，称为源极，漏极和栅极。其制作工艺为在半导体硅的源极和漏极区域中植入带电离子，在二者中间创建一个绝缘层并生长另一个导体形成栅极，根据植入离子的电性不同，分为植入负电粒子的nFET和植入正电粒子的pFET。也就是说，栅极的极性决定了下方绝缘层中电荷的极性，是可以变化的，但是在左右的源极和漏极区域中，电荷的极性是预先植入好的，不能发生变化，因此可以通过改变栅极的极性来形成或断开电荷通路，从而达到开关的效果。</p>
<p>以nFET（植入了负电荷）为例进行说明，在逻辑电路中nFET源极接GND，当栅极接VCC时，源极和栅极间形成了电压差，会吸引硅中的负电荷进入中间的沟道（半导体中正负电荷粒子可以移动），形成了连续的负电荷通路，nFET处于强导通状态；当栅极接GND时会吸引正电荷聚集在沟道，与两侧扩散区中的负电荷形成PN结，阻止任何一个方向的电流流过。</p>
<p>在源极接GND的情况下，可以通过控制栅极的极性与源极形成电压差，使电路导通。反之如果源极接VCC的话，栅极接VCC也不会形成电压差，栅极接GND形成电压差也不能吸引负电荷移动到栅极绝缘层下，无法形成负电荷通路。</p>
<div style="width:70%;margin:auto">
<img src="/posts/e29e40ff/FET.svg" class="" title="nFET">
</div>
<p>因此nFET源极通常接地，当栅极为VCC时导通，pFET源极通常接VCC，当栅极为GND时导通。可以总结出使用FET构建逻辑电路的基本规则：</p>
<ol type="1">
<li>pFET源极必须连接到VCC，nFET源极必须连接到GND</li>
<li>电路输出必须通过一个pFET连接到VCC，电路输出必须通过一个nFET连接到GND</li>
<li>逻辑电路的输出不能同时连接到VCC和GND</li>
<li>逻辑电路使用数量最少的FET</li>
</ol>
<p>由于pFET和nFET的特性，称为互补金属氧化物半导体电路，即CMOS电路，常见的逻辑门需要的晶体管数量为：INV，需要两个晶体管，NOR，NAND，需要四个晶体管。这也可以解释为什么综合出的网表文件中，使用的是NOR和NAND逻辑门，因为这是最基本的逻辑电路门，只能基于此来实现OR和AND逻辑。</p>
<p>CMOS电路的功耗与逻辑门的输出信号频率成正比，与供电电压的平方成正比。</p>
<h3 id="逻辑代数">逻辑代数</h3>
<p>提到了格雷码，卡诺图，以及表示方法Sum Of Product，Product Of
Sum。</p>
<p>逻辑函数可以写为</p>
<ul>
<li>SOP(Sum of Product)的形式，选取所有最小项（函数输出为1）的积</li>
<li>POS(Product of Sum)的形式，选取所有最大项（函数输出为0）的和</li>
</ul>
<p>利用卡诺图，可以通过划定长宽均为2的幂次的长方形来化简逻辑，需要注意卡诺图的四个方向都是循环相接的，例如四个角应当被视作在一个长方形内。</p>
<p>参考资料： 1. <a
href="https://learn.digilentinc.com/Documents/312">Transistors as
Switches</a></p>
<h2 id="数字逻辑电路">数字逻辑电路</h2>
<h3 id="常见元器件">常见元器件</h3>
<p>格雷码的特点是<span class="math inline">\(g(x)\)</span>与<span
class="math inline">\(g(x+1)\)</span>只相差一个位，一个由二进制逐位转为格雷码的方式为：<span
class="math inline">\(g(x)[n]\)</span> = <span class="math inline">\(x[n
+ 1]\)</span> ^ <span
class="math inline">\(x[n]\)</span>，反之也可以逆向转换。</p>
<p>半加器是指不接受进位输入，全加器则接受进位输入，超前进位加法器即使用更复杂的逻辑函数来避免逐级进位产生的电路延迟，但是这也会导致逻辑函数的复杂度随位数快速增大。</p>
<p>SR锁存器即可通过Set,
Reset信号锁存状态，引入clk信号即为同步SR锁存器，进一步为了避免SR信号相同的状态发明了D锁存器。进一步地，通过多状态的锁存和逻辑，可以实现D触发器，在时钟信号的上升沿或者下降沿锁存，也就是说<strong>信号的上升沿和下降沿是通过组合逻辑与锁存的方式来实现的</strong>。位移寄存器可以通过多个D触发器相连的方式实现，每当时钟信号上升沿来临时，前一个触发器的信号就会作为下一个触发器的输入，从而起到位移寄存器的作用。</p>
<h3 id="存储器">存储器</h3>
<ul>
<li>SRAM成本较高，速度较快，1个比特需要6个FET组成。</li>
<li>DRAM成本较低，速度相对较慢，1个比特只需要一个晶体管和电容，但是由于电容上的电荷会泄露，DRAM需要周期性的充电。访问DRAM时，地址先被译码取出行地址，转为独热码使能信号激活，再译码列地址，这样给Row
Hammer这样的攻击留下了可能。</li>
</ul>
<h2 id="可编程逻辑器件原理">可编程逻辑器件原理</h2>
<p>可编程逻辑器件发展历史：</p>
<ol type="1">
<li>由熔丝，紫外线擦除，电可擦除等工艺实现的PROM（可编程只读储存器），一旦编程后不可修改。</li>
<li>可编程逻辑阵列（PAL）和通用逻辑阵列（GAL），可以实现相对复杂的逻辑函数，由与门和或门的阵列组成。</li>
<li>复杂可编程逻辑器件（CPLD），是FPGA的前身，由可编程的逻辑门与若干功能单元组成。</li>
<li>可编程门阵列（FPGA），集成了更多复杂的功能模块，如时钟管理、专用DSP等，也集成了处理器核心和SoC，高速互联总线等。</li>
</ol>
<p>采用可编程的与门或门阵列控制逻辑时，根据逻辑函数的不同，输入通过的门数量不同，导致阵列的输出延时不可控，需要对具体的逻辑函数实现进行分析，不利于确定频率。而FPGA为了解决这一问题采取了查找表LUT(Look
Up
Table)来生成逻辑，LUT采用了SRAM的工艺，存储了输入与输出的直接对应关系，无需通过逻辑门运算，通过直接查表的方式输出，从而确定了延迟。在烧录时，EDA会将这一真值表写入对应的LUT资源中。</p>
<h2 id="vivado集成开发环境设计流程">Vivado集成开发环境设计流程</h2>
<ul>
<li>Post-Synthesis Project 可以导入综合后的网表创建工程。</li>
<li>XDC 约束文件遵循 TCL 命令</li>
<li>Post-Implementation Timing Simulation
综合时考虑到了毛刺等实际问题。</li>
</ul>
<h2 id="verilog-hdl规范">Verilog HDL规范</h2>
<h3 id="发展历史">发展历史</h3>
<p>Verilog硬件描述语言由OVI(Open Verilog
Internatiaonal)组织推动，最早提出的标准为Verilog-1995，经过改进形成标准Verilog-2001，又融合了Verilog-AMS形成了标准Verilog-2005，可以对集成的模拟信号和混合信号系统进行建模。其中还存在另一个Verilog-2005标准是现在的System
Verilog，它是Verilog HDL的超集，是对硬件描述语言和验证语言的集成。</p>
<h3 id="程序结构">程序结构</h3>
<p>最基本的设计单元是模块，通过<code>module</code>和<code>endmodule</code>关键字声明，包括端口声明、数据声明和逻辑功能定义：
*
端口具有三种方向，分别为<code>input</code>，<code>output</code>，<code>inout</code>，<code>inout</code>是由三态门实现的双向端口，需要辅助使能信号控制方向，只能使用<code>assign</code>语句进行逻辑定义。
*
端口可以声明其数据类型为<code>wire</code>或<code>reg</code>，其中输入端口或双向端口只能为<code>wire</code>类型，默认也为<code>wire</code>类型。
* 端口可以声明其数据含义为<code>signed</code>表示为有符号数据。</p>
<p>Verilog中也支持函数这一概念，函数本身与是否实例化为硬件电路并无绝对关系，可以理解为是对代码语言复用的概念，至于是否实例化则却决于复用的代码本身。通过关键字<code>function</code>和<code>endfunction</code>声明，需要对输入进行声明，只能输出一个结果。</p>
<h3 id="描述方式">描述方式</h3>
<p>按照抽象层级划分，Verilog可分为以下四种描述方式：</p>
<ul>
<li>行为级描述：通常用于仿真行为描述，而非硬件实例化，包含的关键字有：
<ul>
<li><code>initial</code>：用于初始化寄存器状态，在综合阶段不起作用，若想实现寄存器初值应当通过<code>reset</code>信号</li>
<li><code>for, while, fork &amp; join</code>：用于仿真阶段的循环，并行</li>
<li><code>event, task</code>：可以通过<code>call</code>触发，用于仿真一些触发行为</li>
<li><code>time, #</code>：设置时间参数，或表示若干时间停顿后继续执行</li>
</ul></li>
<li>数据传输级描述：也就是RTL级别，描述数据的控制流与传输流，是主要的硬件逻辑行为描述层</li>
<li>结构级描述：以<code>module</code>为单位建立整体的结构，相对RTL更加抽象</li>
<li>开关级描述：可以使用硬件原语对门级别进行描述，可以对传输延迟，驱动强度等参数进行声明</li>
</ul>
<h3 id="verilog-hdl要素">Verilog HDL要素</h3>
<p>变量名以下划线或者字母开头，大小写敏感，可以由数字、字母、<code>$</code>、<code>_</code>组成，特别地，由<code>\</code>开头的表示转义标识。</p>
<p>Verilog中实现了一些系统函数和任务，用于仿真测试，常见的有：</p>
<ul>
<li><code>$display("info")</code>：在仿真控制台上显示信息，可以接受格式化字符串</li>
<li><code>$dumpfile("wave.vcd"), dumpvars</code>：将波形输出到文件中</li>
<li><code>load("test_vector.hex")</code>：从文件中加载向量</li>
<li><code>$finish, $random, $realtime ....</code></li>
</ul>
<p>类似于其他语言，在编程过程中可以向编译器提供元信息，通过<code>define</code>进行声明，以指导编译器。也可以通过声明属性的方式进行指导，在变量或者结构前通过<code>(* [attribute] = [constant expression] *)</code>的方式声明其属性，当然编译器并不保证完全遵照属性要求编译。比较常用的属性是<code>mark_debug</code>，可以标记信号，在综合阶段生成调试核后保留该信号以便接入调试，书中还介绍了<code>full_case</code>属性，用于标记<code>case</code>结构，告诉编译器条件完备，避免编译器生成锁存结构应对未列出的情况。其他常见的属性还有<code>optimize_power, fsm_state, dont_touch ...</code></p>
<p>Verilog中的数字常量格式为：<code>[sign][width]['][base][number]</code>，其中sign表示符号，默认为正，bsae表示进制，默认为十进制，当数字超过位宽时截取低位，当数字不足位宽时根据是否表示符号数补0或1（base中若出现<code>s</code>则表示符号数），未指定宽度一般默认为32位。数字常量中的<code>_</code>只起分隔作用，无实际意义，也可以定义值为高阻态Z。</p>
<p>Verilog同时也支持浮点数的常量，转换时遵循四舍五入，负数则取整按照相反数取整结果的相反数计算。Verilog中的字符串常量会将每个字符编码为8bitASCII码进行存储。</p>
<h3 id="verilog-hdl数据类型">Verilog HDL数据类型</h3>
<p>高阻态指的是电路的一种输出状态，既不是高电平也不是低电平。如果高阻态再输入下一级电路的话，对下级电路无任何影响，等效于未接通。而三态门可以实现高阻态这种设计，使得信号在某些情况下处于高阻态。这种设计可以用于实现总线的仲裁、内存控制等，切换到高阻态可以释放总线。通过使用高阻态，电路可以在不需要信号传递时自动进入休眠状态，从而节省能源并提高系统的稳定性。</p>
<h3 id="verilog-hdl表达式">Verilog HDL表达式</h3>
<p>Verilog中绝大多数的运算符如果对含有<code>X</code>或<code>Z</code>的变量进行运算都会得到<code>X</code>的结果，但是<code>===</code>和<code>!==</code>可以对含有<code>X, Z</code>的变量进行比较，区分于<code>==, !=</code>。特别地，在<code>[cond]?A:B</code>表达式中，如果条件<code>cond</code>含有<code>X</code>或<code>Z</code>，那么结果将为<code>A&amp;B</code>。Verilog中也提供了相对复杂的逻辑运算操作，如<code>&gt;&gt;&gt;, &lt;&lt;&lt;</code>算术位移，<code>**</code>幂，<code>*, /, %</code>乘除法，但是要注意系统自动生成的硬件复杂度。Verilog也提供连接操作符，通过<code>&#123;&#125;</code>对定位宽变量进行连接，可以使用<code>&#123;[num]&#123;[var_list]&#125;&#125;</code>将表达式复制相连。需要特别注意的是，<code>+, -</code>默认的结果宽度为<code>Max(width(A), width(B))</code>。</p>
<h2 id="数字系统设计和实现">数字系统设计和实现</h2>
<p>介绍了一些常用的模块实现：</p>
<ul>
<li>在<code>module</code>关键字前声明属性<code>use_dsp48="yes"</code>可以使用DSP48进行加速运算</li>
<li>可以使用<code>generate</code>例化生成，实现类似于 Chisel
的效果，可以声明例化变量<code>genvar</code></li>
<li>ROM, RAM 直接声明为 reg
数组即可，可以通过声明属性<code>rom_style="block"</code>来选择实现方式</li>
<li>XDC约束文件可以通过EDA自带的 IO Planning
功能生成，根据手册连接到需要的端口上</li>
</ul>
]]></content>
      <categories>
        <category>Book Note</category>
      </categories>
      <tags>
        <tag>Vivado</tag>
        <tag>Hardware design</tag>
      </tags>
  </entry>
</search>
